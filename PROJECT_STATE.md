# Project State — Toussaint Workbook

## Current phase
**Everything in the original curriculum and architecture design is now built and validated (batches 13-17): all blocks' modules (69 modules, ~226 est. hours, including the two cumulative-review modules REV1-01/REV2-01), all 13 cheat sheets, all 7 block milestone exams, the Cumulative Final Assessment, the Anki export, Pagefind full-text search, and the print/book export. Nothing from the original design remains unbuilt. This batch (17) has been committed and pushed to the private remote.**

## Resolved (2026-08-12, sixth pass): added MATH-00 (Taylor Expansion From Scratch)
User flagged, after reviewing batch 1, that Taylor expansion was used as a derivation tool in both MATH-01 (chain rule) and MATH-02 (second-order Taylor for Hessians) without ever being taught from scratch, and the user is specifically rusty on it. Added as **MATH-00** (not a renumber of MATH-01-05, to avoid churning already-published IDs) with a from-scratch derivation of both first- and second-order Taylor approximations (first order from the derivative definition directly, second order from matching-derivatives against a quadratic polynomial), two worked examples (sin/cos series, both independently verified), and 4 exercises. MATH-01's prerequisites and readiness check updated to point to it; MATH-01 and MATH-02's prose updated with explicit cross-references back to MATH-00 rather than leaving Taylor expansion as an unexplained tool. `CURRICULUM.md` updated (new row, module/hour counts, a production note explaining the insertion). Tested live, build clean (10 pages).

## Approved decisions
See `docs/decisions/0000-requirements.md`, `0001-corpus-update-and-priorities.md`, `0002-phase2-and-pilot-approval.md` for full records. Summary:
- Paths: `VAULT_ROOT=/Users/julialg2002/PhD-Literature`, `PDF_SOURCE_DIR=../original notes`, `PROJECT_DIR=workbook/`, `CONTENT_OUTPUT_DIR=../generated-notes/`.
- Fully private/local, no deployment, no API keys.
- Grading: deterministic (client-side) + symbolic via a **local SymPy sidecar server** (user chose full automation over rubric-only fallback) + rubric self-check + clipboard "Review with Claude".
- External sourcing finalized: Sutton & Barto, Lynch & Park *Modern Robotics*, Siciliano et al. *Robotics: Modelling, Planning and Control*.
- Output: both interactive Astro site and printable/book export.
- No deadline; estimated hours per module instead.
- User background: rusty calculus/linear algebra (step-by-step re-derivation needed), no C/C++, strong Python, basics-only in optimization/control/RL, weakest in ODEs/dynamics, wants manual MLP↔PyTorch bridge. Strong (not rusty) in open-chain kinematics/Jacobians/rotations (Edinburgh teaching background) — paced as revision except quaternions (confirmed weak spot, full-length).
- User's PhD research (CDT-D2AIR Year-1 report): "Learning Representations of Reachable Behaviour for Planning in Long-Horizon Robotics Tasks," supervised by Dr. Steve Tonneau — TAMP, trajectory optimization/optimal control, MPC, predictive/world models, RL, goal-conditioned policies, manifold/constrained sampling. Full detail in memory `toussaint_workbook_project.md`.
- Priority tiers confirmed (`AUDIT_REPORT.md` §6b): grasping/legged locomotion elevated; SLAM demoted to reference-only; propositional/first-order logic + PDDL/MLN elevated (scoped to TAMP); tree search kept as MCTS prerequisite; general CSP-solving low priority.
- Curriculum (`data/curriculum/CURRICULUM.md`, 13 blocks/~63 modules) and architecture (`data/curriculum/ARCHITECTURE.md`) approved as proposed, no changes requested.
- Pilot module: **ML-03** (Neural Networks: Manual Forward/Backward Pass ↔ PyTorch) built first; KIN-02 (quaternions) is next once ML-03's style is approved.

## Completed — Phase 3 (pilot)
- **Astro + TypeScript site scaffolded** at `workbook/` (merged into existing project files, nothing overwritten). MDX, KaTeX (build-time via `remark-math`/`rehype-katex` through `markdown.processor`, client-side via `katex/contrib/auto-render` for JSON-sourced exercise text).
- **Content collections** (`src/content.config.ts`): `course`, `questions`, `solutions`, `cheatsheets`, matching `ARCHITECTURE.md`'s schemas. Custom multi-entry JSON loader written for `questions`/`solutions` (one file per module containing an array — the built-in `file()` loader doesn't support that shape).
- **ML-03 authored in full**: motivation, objectives, prereq readiness check, notation box, worked example (forward + backward pass, hand-verified numbers), 6 exercises spanning short-text/derivation/symbolic/numeric/code/proof answer types, summary, retrieval questions, mastery checklist, source citations. Citations distinguish source-adapted (2 exercises, from `lecture-maths` Ex.4/5, original numbering preserved) from newly-authored (rest).
- **All worked-example and exercise numbers independently verified**: NumPy analytic + numeric-gradient-check, cross-checked against real PyTorch `autograd` output (installed CPU-only PyTorch in `.venv` for this) — every number in the lesson matches to float32 precision.
- **Grading components built and tested live**: `ExerciseCard.astro` dispatches by answer type. Deterministic numeric checker tested (correct + incorrect). Symbolic checker tested against the running local SymPy server via a real browser fetch (correct exact match, correct equivalent-but-differently-shaped match, correct rejection). Rubric "I've attempted this" flow tested. Review-with-Claude clipboard button implemented; clipboard write itself couldn't be confirmed in this sandboxed browser tool (likely environment permission issue, not a code defect) — **user should verify this one interaction themselves**.
- **Bug found and fixed during testing**: `sympy.simplify()` alone failed to recognize a numerically-equal-but-differently-shaped expression (`exp(-z)/(1+exp(-z))**2` vs `1/(4*cosh(z/2)**2)`) as equivalent. Fixed by switching the symbolic checker to numeric sampling at 25 random points as the primary equivalence test.
- **Bug found and fixed during testing**: progress was only being recorded on *correct* answers, contradicting the "attempted-work-based progress" requirement. Fixed — any genuine (non-empty) attempt now records progress and unlocks the solution, regardless of correctness.
- **Progress storage** (`src/lib/progress/store.ts`, IndexedDB) tested live: attempts recorded correctly for both pass/fail cases, module status auto-derived to "in-progress".
- **Cheat sheet** (`neural-networks-backprop`) authored and linked bidirectionally from ML-03.
- **Production build verified**: `npm run build` succeeds cleanly, 3 static pages, 0 errors.
- Two real bugs were also hit and fixed purely from getting the toolchain working (not content bugs): Astro 7's `mdx({remarkPlugins})` is silently deprecated/non-functional — plugins must go through `markdown.processor`; and raw LaTeX braces in MDX prose need `remark-math` specifically to avoid being misparsed as JSX expressions, not just for rendering.

## Resolved after user review of the pilot (2026-08-12, third pass)
- **Real bug found and fixed**: `rehype-katex` bundled its own separate copy of KaTeX (0.16.47) distinct from the directly-installed one (0.18.4), and the two versions use different CSS class-naming conventions (`sizing` vs `katex-sizing`). Build-time-rendered math (all MDX prose, including the cheat sheet's equation table and part of ML-03's own worked example) was rendering with unscaled sub/superscripts as a result, since `katex.min.css` only matched one of the two conventions. Fixed via an `npm overrides` pin (`"katex": "$katex"`) forcing a single KaTeX version across the whole dependency tree, confirmed with a full clean reinstall (deleted `node_modules`/`package-lock.json`). Verified fixed on both the cheat sheet and ML-03 pages; production build still succeeds.
- Root cause of the user's other report (raw `$...$` text, dead buttons) was confirmed to be opening the `.mdx` source file directly (`file://`) instead of through the dev server, not a real defect. `README.md` added with explicit "don't double-click the file" setup/run instructions.
- **Progress export/import UI added**: `store.ts`'s `exportProgress`/`importProgress` functions existed since the pilot build but had no UI. Added `src/components/ProgressControls.astro` (download-as-JSON / upload-to-restore) on the homepage, plus a "Where your answers live" explainer (IndexedDB, browser-local, never touches git-tracked files). Export tested live and works; import wasn't independently exercised with a real file upload in the automated browser tool (same class of limitation as the earlier clipboard-copy caveat), but the code path is small and symmetric with the tested export path.
- **Local git repo initialized** at `workbook/` (not the parent folder), so the source PDFs (`../original notes/`) are structurally outside the repo entirely. `.gitignore` additionally excludes `data/source-manifest/raw-text/` and `html-text/` (full extracted text of Toussaint's copyrighted lecture notes) even though they're inside `workbook/`. Initial commit made, 38 files, no remote configured yet, nothing pushed anywhere.
- Style note from user: avoid em/en-dash asides going forward, prefer colons/semicolons/commas/parentheses. Applying from here on.

## Resolved (2026-08-12, fourth pass): repo made public, KIN-02 built
- **Repository is now public**: `https://github.com/julialopezgomez/toussaint-workbook`, at user's explicit request. See `docs/decisions/0003-public-repo.md`. README has a prominent "Credit and source material" section attributing Marc Toussaint with a link to his teaching page, before anything was pushed.
- **KIN-02 (Quaternions: Exponential/Log Maps, Interpolation & Jacobians) built as the second pilot module**, full-length per the Phase 2 plan (user's confirmed weak spot). 5 exercises (short-text/derivation/symbolic/numeric/proof), all worked-example and exercise numbers independently verified with NumPy (including a finite-difference check on the angular Jacobian) before writing them into the lesson. Tested live: numeric checker ✓, symbolic checker (including a different valid algebraic form, `(1-q0**2)**0.5` vs `sqrt(1-q0**2)`) ✓, KaTeX rendering (174 spans) ✓. Production build clean, 4 pages.
- **Real bug found and fixed**: after adding KIN-02, the dev server briefly served an empty `<article>` for its page. Traced to leftover duplicate `astro dev` processes from earlier in the session competing for/rebinding ports; NOT a content or code bug (the production build had rendered KIN-02 correctly the whole time). Fixed by fully clearing all stale processes on ports 4321-4325 before restarting. Worth remembering for future sessions: always `lsof -ti:<port> | xargs kill -9` before restarting the dev server if multiple restarts have happened in one session.

## Completed — Phase 4, batch 1 (2026-08-12, fifth pass)
- **Block MATH is now complete**: MATH-01 (Functions/Derivatives/Chain Rule), MATH-02 (Gradients/Jacobians/Hessians/Taylor), MATH-03 (Vector Spaces/Bases/Linear Maps), MATH-04 (SVD & Eigendecomposition, source-adapted from `svd` note), MATH-05 (Covariant Gradient & Steepest Descent). 18 exercises total across the 5 modules, spanning short-text/derivation/symbolic/numeric/proof answer types.
- All worked-example and exercise numbers independently verified with NumPy/SymPy before writing them into lessons (chain-rule values, quadratic-form gradients/Hessians, change-of-basis coordinates, SVD singular values via the $A^\top A$ eigendecomposition route, steepest-descent directions under a non-Euclidean metric, cross-checked in one case against a 200,000-sample random search).
- **Exercise-overlap caught and fixed before publishing**: MATH-02's originally-scoped exercises (`lecture-maths` Ex.4/5, "Backprop in a Neural Net") were already used in ML-03. Rewrote MATH-02 with new exercises covering the same gradient/Jacobian/Hessian mechanics instead of duplicating ML-03's material; updated `CURRICULUM.md`'s MATH-02 row and added a production note there.
- Homepage updated to list all 7 modules in curriculum block order (not alphabetical) with a running total-hours count.
- Tested live: numeric checkers (3 different modules) ✓, symbolic checker with an alternate valid algebraic form (`s+s` vs `2*s`) ✓, KaTeX rendering (133 spans on MATH-04, correct `katex-sizing` class) ✓. Type check and production build both clean (9 pages, 0 errors).
- Pushed to the public repo.

## Completed — Phase 4, batch 2 (2026-08-12, seventh pass)
- **OPT-01 through OPT-05 built**: Newton's method (derived from the 2nd-order Taylor model), Gauss-Newton (derived from the exact least-squares Hessian, dropping the residual-curvature term), KKT/Lagrangian (derived via the parallel-gradients geometric argument), convexity/LP/QP/log-barrier (includes a proof that convex local minima are global), SGD/momentum/Adam/No-Free-Lunch. 17 exercises total.
- **OPT-06 (Bayesian Optimization & Bandits) deliberately deferred**: depends on PROB-01 (Probability Foundations), which doesn't exist yet. Noted in `CURRICULUM.md`.
- All worked-example numbers verified with NumPy/SymPy first (Newton step exactness on a quadratic, Gauss-Newton exactness on a linear residual, a full KKT linear-system solve, log-barrier gradient blow-up near the boundary, SGD mini-batch noise vs. full-batch gradient).
- Tested live: numeric checkers (OPT-01, OPT-03) ✓. Type check and production build both clean (15 pages, 0 errors).
- Pushed to the public repo.

## Completed — Phase 4, batch 3 (2026-08-12, eighth pass)
- **PROB-01 through PROB-06 built**, completing Block PROB: probability foundations/Bayes' rule, Monte Carlo/importance sampling, entropy/cross-entropy/KL (source-adapted from `entropy` note), energy-based views/Boltzmann distribution (source-adapted from `energy` note), Gaussian identities (source-adapted from `gaussians` note), Laplace approximation (built explicitly on MATH-00's Taylor expansion, closing the loop between three earlier modules). 21 exercises total.
- Leveraged verbatim text already read from the `entropy`/`energy`/`gaussians` HTML notes during Phase 1 for higher-fidelity source-adapted content than the pure-paraphrase modules in MATH/OPT.
- All worked-example numbers verified with NumPy/SymPy first (Bayes' rule base-rate example, importance-sampling identity, entropy/cross-entropy/KL values, Boltzmann probabilities, Gaussian product mean/variance, Laplace approximation variance on a genuinely bimodal test density).
- Tested live: numeric checkers (PROB-03, PROB-06) ✓. Type check and production build both clean (21 pages, 0 errors).
- `CURRICULUM.md` updated: OPT-06 (Bayesian Optimization & Bandits) is now unblocked, since it needed PROB-01.
- Pushed to the public repo.

## Repository visibility: made public, then reverted to private (2026-08-12, ninth pass)
- User asked to embed Toussaint's actual source figures (not just original redrawn diagrams) into module content, while the repo was still public. Flagged that this would be direct, non-transformative reproduction of copyrighted diagrams under public redistribution, materially different from the already-public paraphrased/attributed text. User's initial premise ("I'm not distributing my repo, it's private") didn't match the repo's actual state (public, per the fourth-pass entry above); once pointed out, user chose to flip the repo back to **private** (`gh repo edit --visibility private --accept-visibility-change-consequences`) rather than keep it public without real figures.
- Repository is now **private** again. `CLAUDE.md` updated with an explicit instruction not to make it public again without re-checking with the user first. Full record in `docs/decisions/0003-public-repo.md`'s "Reversal" section.
- This unblocked embedding real source figures directly (`src/components/SourceFigure.astro`, new), used starting with the comprehensive MATH revision below.

## Completed — Phase 4, comprehensive MATH block revision (2026-08-12, ninth pass)
- User flagged that Block MATH's batch-1 content (verified-correct but thin) skipped real content: no formal Gaussian definition example (a PROB issue, deferred), no formal PSD/PD matrix definition, no explanation of Newton's method from scratch (an OPT issue, deferred), and generally paraphrased around the source rather than covering its actual depth, missing entire sections (Derivatives with Matrices, eigendecomposition/power method), real figures, and roughly 15 previously-unused source exercises. Explicit ask: revise so the workbook is comprehensive start-to-finish without needing to consult Toussaint's notes directly.
- Re-read `Lecture-Maths.pdf` pages 5–46 as rendered page images (not just extracted text) to catalog every missing Definition/Theorem/Algorithm box, figure, and exercise against the existing MATH-00–05 modules.
- **Block MATH expanded from 6 to 8 modules**: MATH-02B (Matrix Calculus: Identities, Warm-Ups & Applied Examples — ridge regression derived from scratch, Identities 2.3–2.5, GP-regression and logistic-regression worked examples, a Gauss-Newton robotics-IK capstone) split out of MATH-02; MATH-03B (Scalar Products, Metric Tensors & Orthonormal Bases — the formal PD/PSD matrix definition and Sylvester's criterion, closing the exact gap the user flagged) split out of MATH-03. MATH-04 was expanded substantially in place (SVD → pseudo-inverse → covariance/PCA → eigendecomposition → both power-method variants) rather than split further, to avoid exceeding the two-module expansion already communicated to the user.
- MATH-01 through MATH-05 all substantially rewritten: added formal Definition/Theorem/Algorithm boxes with the source's actual numbering (Definitions 2.3–2.5, 3.1–3.19, Theorem 2 for symmetric-matrix SVD, Algorithm 1 finite-difference check), notation-conventions and continuity/differentiability/smoothness sections, and roughly 15 previously-unused source exercises (skew-matrix cross-product gradient, matrix-equation warm-ups (i)–(iv) and (vi)/robotics Gauss-Newton, GP/logistic-regression worked derivations, pseudo-inverse derivation, PCA variance-maximization proof, PSD-eigenvalue equivalence proof, eigenvector orthogonality/$A^k$/shift-invariance proofs, power-method convergence proof, smallest-eigenvalue power-method variant).
- **Two real figures extracted from the source PDF and embedded** via the new `SourceFigure.astro` component: the chain-rule computation-graph diagram (p.11, in MATH-01) and the SVD project-scale-unproject illustration (p.32, in MATH-04). PDF figures are vector graphics (not embedded raster images), so full-page renders (PyMuPDF, 3x zoom) were cropped in PIL rather than extracted directly.
- All new numeric/symbolic claims (matrix-equation solutions, ridge regression, GP log-likelihood gradient, logistic regression Hessian, Gauss-Newton IK step, pseudo-inverse identities, power method incl. smallest-eigenvalue variant) independently verified with NumPy/SymPy before being written into lessons.
- `CURRICULUM.md`'s MATH block table rewritten for the 8-module structure (hours grew from ~15–18 to ~31–34, reflecting genuine source-depth coverage rather than a paraphrased subset), with a new production note explaining the split rationale.
- Type check (`npx astro check`) and production build both clean, 0 errors, all 8 MATH pages (including the two new modules) render correctly; every new exercise has a matching solution (verified programmatically) and satisfies the content schema (3-hint tuples, numeric `sourceRef.page`).
- Tested live: both embedded figures load correctly (200 OK, full natural resolution) on MATH-01 and MATH-04; KaTeX renders cleanly (265–378 spans across spot-checked pages, no console errors); the new MATH-03B numeric exercise (metric-tensor computation) passes end-to-end through the real grading pipeline (`✓ Correct (within tolerance ±0.01)`).
- **Not yet done**: pushing to the (now-private) remote; user confirmation of the new depth/style before applying the same treatment to Block OPT (Newton's method gap) and Block PROB (Gaussian definition gap), per the user's explicit pacing choice ("MATH block first, then check in").

## Completed — Phase 4, comprehensive OPT + PROB revision (2026-08-12, tenth pass)
- User confirmed the MATH-block depth/style was right and explicitly said not to close the four minor known gaps flagged, and to proceed to OPT/PROB with the same non-skipping rigor.
- **Block OPT (all 6 modules) substantially expanded**, re-reading `lecture-optimization.pdf` pages 5-64 and 71-77, and `lecture-ai.pdf` pages 45-51, as rendered images (no new module split, unlike MATH; all expanded in place):
  - OPT-01: Newton's method now derived properly from root-finding first (the user's explicitly-named missing content), plus the non-positive-definite-Hessian fallback, Levenberg-Marquardt damping, both Wolfe conditions, the exponential-convergence theorem, and the source's own Cholesky-substitution steepest-descent derivation.
  - OPT-02: full Gauss-Newton derivation (PSD/Riemannian-metric interpretation, a robotics path-optimization instance), the BFGS secant equation/update, L-BFGS, and the full conjugate-gradient algorithm with the Polak-Ribière formula.
  - OPT-03: the full 4-condition KKT theorem (previously only stationarity was covered), weak duality, and Slater's strong-duality condition.
  - OPT-04: convex/quasiconvex/unimodal definitions, the log-barrier method's central path and exact relaxed-KKT equivalence, the augmented Lagrangian's multiplier update rule, LP standard form via slack variables, and the SVM QP example.
  - OPT-05: the SGD/episodic-SGD algorithms, the Lipschitz-continuity-based convergence theorem (derived, not just stated), RMSprop/Adagrad/Nesterov, the full Adam algorithm, and the formal No-Free-Lunch theorem.
  - **OPT-06 (Bayesian Optimization & Bandits) built for the first time** (it existed only as a deferred curriculum row since batch 2; never actually produced): UCB1 and its regret bound, the Gaussian Process definition and posterior formulas, and all three acquisition functions (MPI/EI/UCB).
  - ~28 new/reworked exercises across the 6 modules, all new numeric/symbolic claims independently verified with NumPy before writing them into lessons.
- **Block PROB (all 6 modules) revised**, re-reading `lecture-ai.pdf` pages 10-26 and `lecture-maths.pdf` pages 92-96 as rendered images, plus the already-fetched `gaussians`/`entropy`/`energy` HTML notes:
  - **PROB-05 (Gaussians) gained the formal Gaussian density definition (1D and $n$D, moment and canonical form) that was completely absent before, the exact gap the user named explicitly** ("for gaussians, you don't even give me a definition"), plus the joint-Gaussian conditional formula (previously only described qualitatively).
  - PROB-01 gained the formal probability-measure axioms, random-variable definition, and named Bayes'-rule terms (posterior/likelihood/prior/evidence); standard distribution families deliberately excluded since the source itself marks them "not relevant for the lecture/exam."
  - PROB-02 gained the formal Monte Carlo estimator definition and an entirely new particle-approximation section (weighted particles, tied to particle filters and sample-based planning, directly relevant to the user's research).
  - PROB-03 gained the neg-log-likelihood/surprise/optimal-code-length framing with a worked coin-toss example.
  - PROB-06 gained the source's actual Definition 5.10 (Laplace approximation, including its motivating robot-path/collision-detection example) and Definition 5.11 (variational inference); the Fisher information section now honestly notes the source itself leaves that topic as an unfinished "TODO," rather than inventing a fuller derivation the source doesn't provide.
  - PROB-04 was reviewed and found already accurate to its source; left largely unchanged.
- Type check and production build both clean, 0 errors, all 24 pages (8 MATH + 6 OPT + 6 PROB + ML-03 + KIN-02 + cheatsheet + homepage) render correctly; every exercise/solution pair verified programmatically to match, with valid 3-hint tuples and numeric `sourceRef.page` values, across all touched modules.
- Tested live: PROB-05's new Gaussian-definition content renders cleanly (126 KaTeX spans, 0 console errors).
- `CURRICULUM.md` updated for both blocks: OPT grew from ~15-18 hrs to ~28-29 hrs, PROB from ~12-15 hrs to ~18 hrs, both with production notes explaining what changed and why.
- **Not yet done**: pushing to the (private) remote; live browser interaction testing of most of the new OPT/PROB exercises beyond the one PROB-05 spot check (numeric/symbolic checkers not individually re-verified for every new exercise, though the underlying numbers were all independently verified via NumPy before writing).

## Completed — Phase 4, pseudocode formatting fix + real CSS bug (2026-08-12, eleventh pass)
- User flagged (with a screenshot) that algorithm boxes (backtracking line search, Newton, BFGS, CG, log barrier, Adam, UCB1, Bayesian optimization, etc.) had been written as flowing bold-prose paragraphs instead of real pseudocode, unlike the source's actual numbered algorithm boxes. Also asked for a brief quality-revision pass, since this whole MATH/OPT/PROB comprehensive-revision effort had accidentally been run at low reasoning effort instead of high.
- **Converted every algorithm box across MATH-02, all of OPT-01/02/04/05/06 to real fenced-code-block pseudocode** (numbered lines, comments, source's own variable names), each with a figure-style citation caption (`*(Marc Toussaint, `file.pdf`, p.N)*`), matching the pattern already used for `SourceFigure`. Added two algorithm boxes that existed in the source but hadn't been included before (Squared Penalty Method, Augmented Lagrangian, both in OPT-04).
- **Found and fixed a real, independently-discovered bug while verifying the fix**: `npm run build` (not just `npx astro check`, which doesn't run the actual MDX/Rollup pipeline and missed this) failed on OPT-05 with an MDX/acorn parse error, caused by a literal `∇f_i(w)` inside `E{...}` in plain prose (outside math delimiters or a code fence) — MDX's JSX parser tries to interpret bare `{...}` as a JS expression. Fixed by wrapping it in proper `$...$` KaTeX delimiters.
- **Found and fixed a second, more serious bug** (visible in the user's screenshot, not caught by any automated check): Astro applies Shiki syntax highlighting to fenced code blocks by default, using its "github-dark" theme *unconditionally regardless of the page's light/dark mode*, injecting `background-color`/`color` as high-specificity inline styles on `.astro-code` and every per-token `<span>`. This fought with the site's own `pre`/`code` CSS, producing the illegible, near-invisible pseudocode the user saw (pale text on a near-black background with a distracting per-token box). Fixed in `src/layouts/BaseLayout.astro`'s global styles with `!important` overrides that strip Shiki's inline span colors/backgrounds and force the pre block itself to use the site's own `--surface-2`/`--fg` theme variables; verified via computed-style checks in both light and dark mode (correct contrast in both).
- Did a lighter polish pass across the newly-touched modules (starting with OPT-06, the fastest-written one) for redundant phrasing and rough edges; fixed what was found. Given the realistic scope (20 touched modules across MATH/OPT/PROB), this was a targeted polish pass, not a full re-derivation of every module — the underlying content itself (formulas, derivations, exercises) had already been independently numerically verified during authoring, so the "low effort" concern was primarily about presentation/formatting (the pseudocode issue) rather than correctness.
- Type check and production build both clean, 0 errors, all 24 pages.

## Completed — Phase 4, batch 4: Block ODE built (2026-08-12, twelfth pass)
- User confirmed the pseudocode fix was fine and asked to move to the next batch. Built **Block ODE (ODE-01, ODE-02, ODE-03)**, the one confirmed real content gap flagged back in the Phase 1 audit: no Toussaint-authored PDF teaches ODEs from first principles, but the user's own stated weakest background area, and a hard prerequisite for Block DYN.
- **ODE-01 (First-Order ODEs)**: formal ODE/order/solution/IVP definitions, direction-field intuition, separable equations (derived rigorously via the chain rule, not just informal differential-cancellation), the logistic equation worked example (whose solution is literally the sigmoid curve, a deliberate callback to PROB/ML), the integrating-factor method derived from scratch, and the Picard-Lindelöf existence/uniqueness theorem tied directly back to OPT-05's Lipschitz-continuity definition.
- **ODE-02 (Linear Systems, Eigenvalues & Stability)**: the eigenvector solution ansatz for $\dot x=Ax$ derived from scratch (built explicitly on MATH-04's eigendecomposition, not new linear algebra), a worked 2x2 example, the harmonic-oscillator special case for complex eigenvalues, the full stability classification (asymptotic/unstable/marginal, plus the 2D node/saddle/spiral/center taxonomy), and the linearization argument connecting this directly to how robot-dynamics equilibria will be analyzed in Block DYN.
- **ODE-03 (Numerical Integration: Euler & RK4)**: forward Euler derived from a first-order Taylor expansion (closing the loop back to MATH-00), real pseudocode for both Euler and RK4 (learned from the pseudocode-formatting fix earlier this session — written correctly the first time here), a head-to-head worked comparison showing RK4's error is ~450x smaller than Euler's for the same step size and 4x the cost, and a brief, honest note on stiffness/implicit methods without overclaiming a full stability-theory derivation.
- Since no source PDF exists for this block, content is tagged `newly-authored`/`external-adapted` throughout, citing standard textbook treatment (Boyce & DiPrima; Strogatz) generically rather than specific pages, consistent with the block's original `newly-authored` classification in `CURRICULUM.md`. Resolved the standing "ODE external source TBD" open question from Phase 2 (no such PDF is actually available in this project).
- All worked examples independently verified (SymPy for ODE-01/02's symbolic substitution checks; NumPy plus a from-scratch RK4 integration for ODE-02's linear-system worked example and ODE-03's Euler/RK4 comparison).
- Type check and production build both clean, 0 errors, 27 pages total. Tested live: pseudocode renders correctly (same fix from the previous pass held up for entirely new content), and a numeric exercise passes through the real grading pipeline end-to-end (`✓ Correct (within tolerance ±0.001)`).
- `CURRICULUM.md` updated: ODE block hours grew from the original 8–10 estimate to 11.5 to match the depth standard set by MATH/OPT/PROB.

## Completed — Phase 4, batch 5: KIN-01 and KIN-03 built (2026-08-12, thirteenth pass)
- User asked for KIN-01/KIN-03 (the remaining KIN modules, KIN-02/quaternions already existed as the pilot), built at the "revision-paced, compact" depth the block was originally scoped for back in Phase 0 (given the user's Advanced Robotics teaching background at Edinburgh), not the exhaustive from-scratch treatment used for MATH/OPT/PROB.
- **KIN-01 (3D Rotations & Angular Velocity)**: pose/$SO(3)$ definitions, Euler angles/gimbal lock (with a real embedded figure), the rotation vector/Rodrigues' formula/skew matrix/exponential map, $\dot R=\text{skew}(w)R$ derived from the geometric definition of angular velocity, homogeneous transforms, and the forward-frame/backward-coordinate distinction tied explicitly back to MATH-03's contra/covariance work.
- **KIN-03 (Forward/Inverse Kinematics & Jacobians)**: forward-kinematics chaining, the geometric Jacobian-column construction (with a real embedded figure), IK posed as a regularized NLP with its Gauss-Newton closed form deliberately **not** re-derived, since it's the identical formula already derived in MATH-02B's robotics exercise, real pseudocode for the small-step IK algorithm, and the source's own explicit warning against mistaking a scaled IK/Newton step for a real motion-generation policy.
- Two real figures extracted from `Lecture-Robotics.pdf` (gimbal-lock illustration p.15, rotational-DOF Jacobian geometry p.21) via the same PyMuPDF render-and-crop approach used for the MATH block figures.
- `robotkin`'s much deeper dynamics content (Newton-Euler, $M(q)\ddot q+F(q,\dot q)=u$, Euler-Lagrange, the full waypoint→trajectory→controller stack) was read but deliberately left out of KIN-03 as belonging to Block DYN instead, not a kinematics-only module.
- All worked examples independently verified with NumPy (a 2-link planar arm's forward kinematics, Jacobian columns cross-checked against finite differences, and the Gauss-Newton IK closed form).
- Type check and production build both clean, 0 errors, 29 pages. Tested live: both new figures return 200 OK, pseudocode renders with correct contrast (confirming the earlier CSS fix generalizes), and a new exercise passes through the grading pipeline end-to-end.
- `CURRICULUM.md` updated for both modules and the block summary table; this batch was assessed as straightforward (no blockers, no ambiguity needing user input), so per the user's explicit instruction, proceeding directly to Block DYN next without a check-in.

## Completed — Phase 4, batch 6: Block DYN built, all 7 modules (2026-08-13, fourteenth pass)
- Built **DYN-01 through DYN-07**, completing the Dynamics & Control block: Newton-Euler/Euler-Lagrange equations of motion and force kinematics (DYN-01); PD/PID point-mass control and the characteristic-equation damping regimes (DYN-02); inverse/forward dynamics and operational-space control (DYN-03); the Bellman/HJB/Riccati chain, LQR-is-PD-control, and the four-way IK/operational-space/optimal-control/trajectory-optimization unification (DYN-04); cubic splines as closed-form minimum-acceleration optimal control, timing-optimal control, and Hermite/B-spline continuity (DYN-05); controllability (the rank condition on $[B\ AB\ \cdots\ A^{n-1}B]$) and all three stability definitions plus both worked Lyapunov examples, energy and LQR value function (DYN-06); and Model Predictive Control as receding-horizon re-solving of DYN-04's finite-horizon problem, framed explicitly as a DYN-06-style closed-loop policy (DYN-07).
- **Five source-formula discrepancies were found via independent NumPy/SymPy verification and corrected, each disclosed transparently in the affected module's Source note rather than silently fixed or blindly trusted**: DYN-03's operational-space-control second-derivative term (source showed a stray factor of 2 on a cross term, corrected via finite-difference check on a 2-link arm); DYN-04's 1D point-mass ARE closed form (a coefficient discrepancy, corrected via direct symbolic ARE solve verified to machine precision); DYN-05's cubic-spline coefficient `a` (an apparent sign flip, corrected via `sympy.solve` on the exact boundary-value system); DYN-05's timing-optimal-control closed form for $\tau^*$ (did not reproduce under direct numerical verification in either the general or $v_0=0$ case, so the specific formula is deliberately omitted rather than asserted unverified, with an explicit note explaining why); and DYN-06's energy-Lyapunov decay-envelope formula for $\dot V(x)$ (didn't reproduce cleanly, so only the verified sign result $\dot V=-K_d\dot q^2$ is asserted, using the standard total-mechanical-energy Lyapunov function rather than the source's kinetic-energy-only shorthand).
- **DYN-07 is a newly-authored synthesis module**: confirmed via full-text search that `lecture-robotics.pdf` contains no section literally labeled MPC or "receding horizon," so the module derives the receding-horizon principle directly from DYN-04's already-covered finite-horizon trajectory optimization, cross-referencing DYN-06's closed-loop framing and citing standard control-theory MPC literature externally (Rawlings/Mayne/Diehl). **The original curriculum plan called for a direct cross-check against Definition 3 of the user's own PhD Year-1 report; that report was not accessible as a file within this project's approved source directories in this session (searched `PDF_SOURCE_DIR` and the broader vault for it, found nothing), so the cross-check was deliberately omitted rather than fabricated or guessed at** — flagged explicitly in DYN-07's own Source note. If the user supplies the report, this module should be revised to add that specific notational cross-check.
- Real pseudocode used throughout (the Newton-Euler recursion sketch, the MPC receding-horizon loop) matching the established fenced-code-block-plus-citation convention; two figures embedded (damping-regime plot in DYN-02, joint-vs-operational-space diagram in DYN-03), reusing extraction work already done; no new figures were extracted for DYN-04 through DYN-07 (judged not to add clarifying value beyond what ODE-02 already established without figures, e.g. the Lyapunov δ-ε phase-portrait diagrams in DYN-06's source were assessed as decorative rather than essential, since the text/equations already convey the definitions precisely).
- `CURRICULUM.md`'s DYN block table and production note updated (hours grew from the original ~19.5 hr estimate to ~27 hrs, matching the depth-driven growth pattern seen in every other comprehensively-built block).
- Type check (`npx astro check`) and production build both clean, 0 errors, all 36 pages render (including all 7 DYN modules); every DYN exercise/solution pair validated programmatically (id matching, 3-hint tuples).
- Tested live: DYN-06 and DYN-07 pseudocode renders with correct light-mode contrast (`rgb(240,240,245)` background / `rgb(26,26,46)` text, matching the established CSS fix); KaTeX renders correctly (confirmed via rendered `𝜋_MPC(x,t)`-style output, not raw LaTeX); all four DYN-06 exercise cards render with correct input types; DYN-06's eigenvalue-stability numeric exercise passes end-to-end through the real grading pipeline (`✓ Correct (within tolerance ±0.01)`).
- **Not yet done**: committing/pushing this batch to the private remote (still outstanding from every earlier batch too); dark-mode pixel verification for DYN-06/07 specifically wasn't completed (the theme-toggle mechanism wasn't quickly discoverable via script in this pass), though the underlying CSS fix is global and unrelated to this batch's content, so this is a low-risk gap, not a new regression.

## Resolved (2026-08-13): real KaTeX rendering bug found by user, fixed and swept site-wide
- User spotted (via a screenshot of DYN-04 in their own browser) a block of raw red LaTeX source text rendering where the "grand unification" four-way aligned equation should have been.
- **Root cause diagnosed and confirmed via a minimal `unified`/`remark-math`/`rehype-katex` repro script**: `remark-math` requires the closing `$$` of a multi-line display-math block to sit alone on its own line; DYN-04's block had `\end{aligned}$$` glued onto the last content line instead. This silently fails to parse as math (not a build error, since `rehype-katex` has `throwOnError:false`), so `rehype-katex` renders the entire block as a raw-text `katex-error` span. Neither `npx astro check` nor `npm run build` catch this class of bug (both report "0 errors" regardless), since it's a silent runtime-rendering fallback, not a compile failure.
- **Fixed DYN-04** (closing `$$` moved to its own line) and **grepped every `.mdx` file in the repo** for the same pattern (a `$$` line with trailing content, or a closing `$$` glued onto content) — DYN-04 was the only offender. Confirmed via a full rebuild: `grep -rl katex-error dist/` now matches nothing in any actual page (only a harmless match inside KaTeX's own library JS bundle, which defines the class name as part of its own error-handling code). Verified live: no error span, math renders as a proper 4-row equation table.
- **New standing verification step, added to this project's process going forward**: after any batch touching multi-line `$$...$$` blocks, grep the touched files for `\$\$` lines with trailing content, and/or grep the built `dist/` output for `katex-error`, since this class of bug is otherwise invisible to both the type checker and the build.

## Completed — Phase 4, batch 7: Block PLAN built, all 4 modules (2026-08-13, fifteenth pass)
- User asked to proceed to the next batch of modules; built **Block PLAN (PLAN-01 through PLAN-04)**, the Path & Motion Planning block, at the same comprehensive depth as MATH/OPT/PROB/DYN.
- **PLAN-01 (Configuration Space & Geometric Feasibility)**: formal $\mathcal{X}$/$\mathcal{X}_\text{fea}$ definitions (sourced from Marc Toussaint's own `robotkin` HTML note's "Fundamental Kinematics Concepts" section, previously unused by KIN-01/KIN-03), degrees-of-freedom/minimal-coordinates, the feedback-control/trajectory-optimization/path-finding triad (explaining why the first two are both "local" methods), potential fields (derived to be exactly DYN-02's P control in gradient-descent form, inheriting its local-minimum problem), and wavefront/Dijkstra grid search (motivating the curse of dimensionality that sampling-based methods fix).
- **PLAN-02 (Sample-Based Path Finding)**: PRM (real generation pseudocode, local planner/resolution parameter, the probabilistic-completeness bound with a verified numeric exercise showing it goes vacuous for a narrow-passage clearance, Gaussian/bridge sampling), RRT (real simplest and goal-biased pseudocode, bi-directional search, RRT* mention), and a PRM-vs-RRT comparison. Two figures embedded (local-planner resolution diagram, an RRT growth-tree screenshot).
- **PLAN-03 (Non-Holonomic Systems)**: formal holonomic/non-holonomic constraint definitions (also from `robotkin`'s "Fundamental Kinematics Concepts," including the integrable-constraint/Pfaffian aside), Toussaint's own tangent-space definition ($\dim(U_x)<\dim(x)$), the car (bicycle) model with its differential constraint derived and independently verified via SymPy, control-based sampling and RRT-with-differential-constraints (real pseudocode), configuration-space metrics (SO(2)/SO(3)/quaternion, cross-referencing KIN-02), and Dubins/Reeds-Shepp curves. Two figures embedded (a bad-vs-good car-path comparison pair showing why naive PRM/RRT paths can be physically infeasible).
- **PLAN-04 (Path Optimization)**: a deliberately short, purely connective module (per its own curriculum-scoped role) explaining why trajectory optimization alone can fail via local minima (PLAN-01's local-minimum problem one level up, in trajectory space) and why the standard two-stage pipeline (sample-based path finding for global topology, then DYN-04's trajectory optimization for local refinement) fixes it; does not re-derive DYN-04's cost function, only connects to it. Closes with a capstone-style synthesis exercise tracing the full PLAN/DYN pipeline for a non-holonomic robot.
- Four real figures extracted from `Lecture-Robotics.pdf` via the established PyMuPDF render-and-crop approach (these four happened to be embedded raster images already, not vector graphics, so no full-page-render-then-crop was needed, just direct image extraction at each figure's bounding box).
- All worked claims independently verified before writing (car differential constraint via SymPy; PRM completeness bound via direct Python computation at two clearance values, showing the vacuous-vs-near-certain contrast).
- Applied the new katex-error verification step (see above) to this batch specifically: grepped all four new `.mdx` files for the closing-`$$`-placement bug before building; none found. `npx astro check` and `npm run build` both clean, 0 errors, 40 pages total (up from 36). Confirmed via `grep -rl katex-error dist/` that no page in the entire site has a rendering failure.
- Tested live: all four new figures return 200 OK; PLAN-03's dim($U_q$) numeric exercise passes end-to-end through the real grading pipeline (`✓ Correct (within tolerance ±0)`); pseudocode renders with correct contrast (3 code blocks confirmed on PLAN-02).
- `CURRICULUM.md`'s PLAN block table and a new production note updated (hours grew from the original ~8 hr estimate to ~12 hrs, consistent with every other comprehensively-built block); `PROJECT_STATE.md` updated with this entry.
- **Not yet done**: committing/pushing this batch (or any prior batch) to the private remote, still outstanding from every earlier batch.

## Completed — Phase 4, batch 8: Block MANIP built, both modules (2026-08-13, sixteenth pass)
- User asked for "next batch" again; built **Block MANIP (MANIP-01, MANIP-02)**, Tier 2, deliberately scoped to this block's own "naturally light" framing (source material is a brief, explicitly-skipped-that-term intro, not a full sub-course).
- **MANIP-01 (Grasping Fundamentals)**: formal contact/friction-cone/force-closure definitions (sourced primarily from `lecture-robotlearning`'s more rigorous "Grasping Background" section rather than `lecture-robotics`'s own brief version), the Mishra-Schwartz-Sharir existence result, a wrench-polytope quality metric, form closure/caging/equilibrium distinctions, and the mitten thought experiment. Two figures embedded (friction cones, the mitten cartoon).
- **MANIP-02 (Legged Locomotion Basics)**: statically-stable walking (support polygon), the Zero Moment Point (explicitly connected to DYN-03's fully-actuated operational-space control via the "glued foot" argument), and passive dynamic walking (the Compass Gait's swing-phase dynamics shown to be exactly DYN-01's general form with $u=0$, standard impact-model assumptions, brief forward-pointer to policy-gradient walking). One figure embedded (Compass Gait mechanical model).
- All numeric claims independently verified (a 2D wrench computation, a barycentric-coordinate support-polygon check, both via NumPy).
- Applied the katex-error verification step from the previous pass: grepped both new `.mdx` files for the closing-`$$` bug before building (none found), and confirmed via `grep -rl katex-error dist/` that no page in the entire site (42 pages now) has a rendering failure. `npx astro check` and `npm run build` both clean, 0 errors.
- Tested live: all three new figures return 200 OK; both a MANIP-01 and a MANIP-02 numeric exercise pass end-to-end through the real grading pipeline.
- `CURRICULUM.md`'s MANIP block table and a new production note updated (hours grew modestly from ~4.5 to ~5.5, consistent with the pattern); `PROJECT_STATE.md` updated with this entry.

## Completed — Phase 4, batch 9: Block ML built, all 6 remaining modules (2026-08-13, seventeenth pass)
- User asked for "next batch" again; built **ML-01, ML-02, ML-04, ML-05, ML-06, ML-07** (ML-03 already existed as the Phase 3 pilot), completing Block ML.
- **ML-01 (Computation Graphs & Autodiff)**: formalizes what ML-03 had assumed all along, partial vs. total derivatives, computation graphs as DAGs, forward-mode and backward-mode chain rules, and the complexity argument for why backprop (backward mode) is used for NN training (cost scales with output dimension, not parameter count).
- **ML-02 (Regression & Classical Supervised ML)**: non-linear features, estimator variance/overfitting, ridge vs. lasso, k-fold cross-validation, and logistic regression derived from the discriminative-function/softmax framing through to its gradient. **Found and fixed a real dangling cross-reference**: MATH-02B said "recall the logistic regression gradient from ML-03," but ML-03 never covers logistic regression at all (only MLP forward/backward with square loss); the link now correctly points to ML-02, which supplies that gradient.
- **ML-04 (NN Architecture Essentials)**: linear/nonlinear operator catalog (FC, convolution, ReLU, pooling, softmax, layer-norm), a conceptual survey of CNN/LSTM/Transformer's structural assumptions, initialization's variance-preservation argument (connected explicitly to ODE-02/DYN-06's stability framing), and dropout/weight-decay regularization. Deliberately cross-references OPT-05 for SGD/Adam rather than re-deriving.
- **ML-05 (Kernelization & Bayesian Learning)**: the kernel trick derived from ridge regression via the Woodbury identity, the RKHS/representer theorem, and — the module's central result — ridge regression derived as Bayesian MAP/posterior inference, making precise (not just asserted) that a Gaussian Process *is* Bayesian kernel ridge regression. This supplies the derivation [OPT-06](/course/OPT/OPT-06) assumed when it stated the GP posterior formulas for Bayesian optimization. Also covers Bayesian logistic regression/GP classification via the Laplace approximation, reusing MATH-02B's and PROB-06's already-derived machinery.
- **ML-06 (Unsupervised Learning)**: PCA derived from projection-error minimization (explaining *why* MATH-04's covariance-eigendecomposition connection holds, not just asserting it), a brief survey of the dimensionality-reduction "zoo" (kernel PCA as ML-05's kernel trick applied to PCA, spectral clustering, MDS, autoencoders), and k-means/GMM-EM derived with the limit connecting them made explicit.
- **ML-07 (Advanced Topics)**: local/weighted regression (as weighted ridge regression), bagging/boosting/AdaBoost, gradient boosting framed explicitly as function-space gradient descent (connecting to OPT-01/02), dropout's approximate-Bayesian justification (Gal & Ghahramani's dropout-as-Deep-GP result, connecting ML-04 and ML-05), and the ML-specific reading of OPT-05's already-proven No-Free-Lunch theorem. Built at an appropriately lighter Tier-3/survey depth, matching the source's own "optional/advanced" framing for most of this chapter's content.
- Deliberate cross-referencing discipline throughout: every module was checked against MATH-02B, MATH-04, OPT-05, OPT-06, PROB-01, PROB-03, PROB-05, and PROB-06 before writing, to identify exactly what was already derived elsewhere (never re-derived) versus genuinely new (the actual content added). This is more cross-reference-checking than any prior batch needed, since ML overlaps structurally with nearly every earlier block.
- All numeric/symbolic claims independently verified before writing (SymPy: polynomial-kernel feature-map identity, logistic-regression gradient, partial/total-derivative example; NumPy: Bayesian-posterior-equals-ridge-estimate, k-means iteration, RBF feature value, AdaBoost weight, conv-vs-FC parameter count).
- Applied the katex-error verification step from prior passes: scanned all 6 new `.mdx` files for the closing-`$$` bug before building (none found), confirmed via `grep -rl katex-error dist/` that no page in the entire site (48 pages now) has a rendering failure. `npx astro check` and `npm run build` both clean, 0 errors.
- Tested live: ML-05 renders with no KaTeX errors and all 4 exercise cards present; a polynomial-kernel numeric exercise passes end-to-end through the real grading pipeline; MATH-02B's fixed link was confirmed live to correctly point to `/course/ML/ML-02`.
- `CURRICULUM.md`'s ML block table and a new production note updated (hours grew from ~19 to ~23, consistent with the pattern); `PROJECT_STATE.md` updated with this entry.

## Completed — Phase 4, batch 10: Block RL built, all 7 modules (2026-08-13, eighteenth pass)
- User asked for "next batch, go ahead with RL"; built **RL-01 through RL-07**, completing the Reinforcement Learning Foundations block.
- **RL-01 (MDPs, Value Functions & Bellman Equations)**: formal MDP/Markov-property definitions, expected return/value function/Q-function, the recursive value property derived, and the Bellman optimality equation explicitly shown as DYN-04's deterministic Bellman equation generalized (stochastic transitions, reward-maximization sign convention, discounting).
- **RL-02 (Value Iteration, Q-Iteration & Convergence)**: both algorithms derived from the Bellman optimality equation, plus a full contraction-mapping convergence proof, presented in complete detail as the template RL-03 explicitly reuses.
- **RL-03 (TD Learning & Q-Learning)**: both derived by replacing RL-01/RL-02's exact expectations with single sampled transitions; Q-learning's convergence explained as a stochastic approximation riding on RL-02's proof; brief coverage of $n$-step updates, eligibility traces deliberately skipped (matching the source's own "impractical outside tabular settings" framing).
- **RL-04 (Policy Gradient & Deep RL)**: the Bellman residual loss (Q-learning generalized to an SGD-trainable regression loss, cross-referencing ML-02/ML-04/OPT-05 rather than re-deriving), and the full policy gradient theorem derivation via the log-derivative trick through the trajectory-probability factorization (the environment's own dynamics shown to drop out of the gradient entirely). Includes a robotics-flavored "why Q not V" note connecting to DYN-03/DYN-04's HJB equation.
- **RL-05 (Exploration Strategies)**: the model-based/model-free/policy-search taxonomy, optimistic initialization, UCB-style exploration (explicitly identified as OPT-06's UCB1 applied to Q-values), R-max's strictly-optimistic mechanism (PAC-MDP), KWIK-R-max, and Bayesian RL/belief planning.
- **RL-06 (Monte Carlo Tree Search & UCT)**: the block's largest module — a compact tree-search primer (BFS/DFS/iterative-deepening complexity table), the MDP/POMDP/game decision-tree taxonomy, the progression from flat Monte Carlo through 1-step UCB to full sequential UCB/MCTS (real pseudocode throughout), and UCT shown explicitly to be **OPT-06's UCB1 formula run independently at every tree node** (the module's central result, directly relevant to the user's own MCTS-based research literature per project notes), plus minimax/negamax and evaluation functions/AlphaGo.
- **RL-07 (Decision Theory & Value Alignment)**: kept deliberately lighter per its own curriculum scope. The basic decision-theoretic formalism, risk aversion/seeking, decision networks shown to unify both RL-01's MDPs and ML-02's empirical risk minimization as the same formalism, information gain, the curiosity/intrinsic-motivation "white noise TV" failure mode and its predictor-improvement fix, and value alignment at a conceptual level.
- Heavy cross-referencing discipline again (as in the ML batch): RL-01 cross-references DYN-04; RL-04 cross-references ML-02/ML-04/OPT-05; RL-05/RL-06 cross-reference OPT-06's UCB1 (never re-derived); RL-06 cross-references PROB-02's Monte Carlo estimator; RL-07 cross-references ML-02 and PROB-02/PROB-03.
- All numeric/symbolic claims independently verified before writing (Python: value/Q-iteration hand-worked examples, UCT node-selection comparison, tree-search node counts, risk-aversion example, optimistic-init value, AdaBoost-style computations reused from context; SymPy: the log-derivative trick and trajectory-factorization identities).
- Applied the katex-error verification step: scanned all 7 new `.mdx` files for the closing-`$$` bug before building (none found), confirmed via `grep -rl katex-error dist/` that no page in the entire site (55 pages now) has a rendering failure. `npx astro check` and `npm run build` both clean, 0 errors.
- Tested live: RL-06 renders with no KaTeX errors, 3 pseudocode blocks and all 4 exercise cards present; the UCT node-selection numeric exercise passes end-to-end through the real grading pipeline.
- `CURRICULUM.md`'s RL block table and a new production note updated (hours grew from ~16.5 to ~20.5, consistent with the pattern); `PROJECT_STATE.md` updated with this entry.

## Completed — Phase 4, batch 11: Block SYM built, all 4 modules (2026-08-13, nineteenth pass)
- User said "continue"; built **SYM-01 through SYM-04**, completing the Symbolic Foundations for TAMP block, the last block feeding directly into task-and-motion planning.
- **SYM-01 (Propositional Logic Essentials)**: kept deliberately narrow per its own curriculum-scoped role (syntax/semantics/entailment/soundness-completeness/Horn-clause Modus Ponens only; full resolution/CNF-conversion deferred to SYM-02's richer FOL treatment rather than duplicated).
- **SYM-02 (First-Order Logic & Inference)**: FOL syntax/semantics, quantifier properties, universal/existential instantiation, unification, Generalized Modus Ponens, forward/backward chaining, and FOL resolution. Worked examples for unification/GMP were deliberately newly-authored rather than reusing the source's own fully-solved "West is a criminal" example, so a complete solution isn't handed to the reader before they attempt it themselves.
- **SYM-03 (STRIPS, PDDL & Relational MDPs)**: MDP states as grounded FOL literals (cross-referencing RL-01), STRIPS/PDDL and the frame assumption, a verified state-space-size argument ($2^{400}\approx10^{120}$ for 20 objects) for why deterministic rules are unlearnable, and Pasula/Zettlemoyer/Kaelbling's probabilistic relational rules with the "uncertainty enables learning" insight, explicitly connected to ML-02's regularization.
- **SYM-04 (PRMs & Markov Logic Networks)**: Probabilistic Relational Models, MLNs (including the Friends & Smokers worked example) and both of their limiting cases (zero-arity predicates recovering standard graphical models; infinite weights recovering classical hard FOL from SYM-02), closing with an explicit statement of the whole block's throughline.
- **Found and fixed a real, previously-undetected class of bug while doing this batch's own verification pass, then swept the entire site for it**: three modules (SYM-04 itself, plus pre-existing **PROB-03** and **RL-03** from earlier batches) had an exercise defined in their `questions/*.json` file with no corresponding `<ExerciseCard id="...">` tag in the module's `.mdx`, meaning that exercise silently never rendered on the page at all despite being fully authored, valid content. Neither `npx astro check` nor `npm run build` catch this (both only validate that JSON exercise IDs and schema are individually well-formed, not that every exercise is actually referenced from its module's prose), and it isn't visible without either reading each module's rendered page closely or writing a dedicated cross-check. Wrote a one-off Python script cross-referencing every `questions/*.json` file's exercise IDs against every `<ExerciseCard>` tag in the matching `.mdx` file, site-wide; found exactly these three cases, fixed all three by inserting the missing card at the appropriate point in each module's prose, and confirmed via re-running the script that the entire site (34 modules) now has 100% exercise/card correspondence in both directions.
- **New standing verification step, added to this project's process going forward** (alongside the existing katex-error dist-scan check): after any batch, cross-check every module's `questions/*.json` exercise IDs against its `.mdx`'s `<ExerciseCard>` tags, since this class of bug is otherwise invisible to both the type checker and the build.
- All numeric/symbolic claims independently verified before writing (Python for the state-space-size and MLN grounding/factor-value computations).
- `npx astro check` and `npm run build` both clean, 0 errors, 59 pages. Tested live: SYM-04 (all 4 exercises now render, one passes end-to-end through the grading pipeline), PROB-03, and RL-03 (both now show their previously-missing exercise, confirmed via live DOM inspection) all verified with no KaTeX errors.
- `CURRICULUM.md`'s SYM block table and a new production note updated (hours roughly steady, ~8 to ~8.5); `PROJECT_STATE.md` updated with this entry.

## Pending — next action needed from user
- **Nothing is outstanding.** Every item from the original curriculum (`CURRICULUM.md`) and architecture (`ARCHITECTURE.md`) design has been built, including the two cumulative-review modules, Pagefind search, and the print/book export, all previously-deferred items the user explicitly asked to complete in batch 17.
- This is now purely a maintenance/study phase: future work is whatever the user wants to add, fix, or extend, per README's "Editing content" section.

## Completed — Phase 4, batch 17: cumulative-review modules + print export + Pagefind search built; full site committed and pushed (2026-08-14, twenty-fifth pass)
- User said "do what's left and commit please" — completing the three previously-deferred items in one batch, then committing everything from batches 13-17.
- **REV1-01 / REV2-01 (the two cumulative-review modules)**: inserted as two new single-module blocks, `REV1` (between SYM and RLEARN) and `REV2` (between RLEARN and CAP), in `src/lib/curriculum/blocks.ts` — chosen over cramming them into an existing block's ID range, since neither naturally belongs to any single preceding block. REV1-01 is a genuine synthesis pass across all 8 preceding blocks (a one-line-per-block table linking to every cheat sheet/exam already built, plus three explicitly-traced cross-block themes: local-vs-global methods, the known-model-to-model-free spectrum, and metric-awareness). REV2-01 is deliberately shorter, since RLEARN-00 already did most of this orientation work at the block's own start; it recaps RLEARN's own "shortcuts to the control stack" throughline and the privileged-teacher pattern's four instances across the block. Both are 100% newly-authored synthesis of already-cited, already-verified content, no new source material. No cheat sheets built for REV1/REV2 specifically (a cheat sheet of a review of cheat sheets adds no value) — noted explicitly as an intentional exception to the "one per block" rule, not an oversight.
- **Print/book export** (`/print`): a single Astro page rendering every module's `<Content/>` in curriculum order via `render()`, with print-specific CSS (`break-before: page` per module) rather than a separate concatenation script, since Astro's own static rendering does the concatenation directly. Exercises deliberately excluded (interactive grading doesn't translate to print). Confirmed via the site-wide `katex-error` scan that concatenating all 69 modules' math onto one page doesn't surface any latent KaTeX issues — the most comprehensive rendering check run all session.
- **Pagefind full-text search** (`/search`, plus `pagefind` added as a devDependency and wired into `npm run build` as `astro build && pagefind --site dist`): indexes the actual built HTML output at production build time (100 pages, ~20,200 words), fully static and local, no server. **Found and fixed a real bug during this build**: the search page's original implementation used a static `import('/pagefind/pagefind-ui.js')` wrapped in try/catch to gracefully handle the dev-mode case (Pagefind's index doesn't exist until a production build) — but Vite's dev-time import-analysis plugin resolves `import()` paths eagerly and 500s the *entire script* when the file doesn't exist, before the try/catch's runtime logic ever executes, even with a `/* @vite-ignore */` comment. Rewrote to load Pagefind's UI bundle via a plain `<script src>` DOM element with `onload`/`onerror` handlers instead (also Pagefind's own documented usage pattern, exposing `window.PagefindUI` globally) — this sidesteps Vite's static module-graph analysis entirely, since a dynamically-created script tag isn't part of the ES module graph Vite inspects. Verified the fix two ways: under `npm run dev`, the script now correctly 404s and shows the "index not found, run a production build" fallback message instead of breaking; under `npm run build && npm run preview`, a live search for "covariant gradient" correctly returned 18 relevant results with MATH-05 ranked first and real matching sub-passages shown.
- README updated with a "Full-text search" section (documenting the build+preview requirement) and an "Other exports" section (`/print`, `/anki`).
- Full validation: `astro check` (0 errors), site-wide `<ExerciseCard>` cross-reference (361/361 exercises matched two-way), solution id-matching (clean), `npm run build` (100 pages + Pagefind index), site-wide `katex-error` scan (0 matches across the entire site including the 69-module-concatenated `/print` page). Live-verified REV1-01/REV2-01 (correct prev/next positioning: REV1-01 between SYM-04/RLEARN-00, REV2-01 between RLEARN-08/CAP-01; 0 KaTeX errors), `/curriculum` (69 modules across 15 of 15 blocks, block-jump order exactly `...SYM, REV1, RLEARN, REV2, CAP`), `/print` (69 articles, MATH-00 first through CAP-01 last, 0 KaTeX errors), and `/search` (both the dev-mode graceful-fallback path and the real production search, end to end).
- **Committed and pushed**: everything accumulated since the last push (batches 13 through 17 — CAP-01, all site navigation, all cheat sheets, all milestone exams, the Anki export, and this batch's review modules/print/search) went into a single commit, reviewed for anything sensitive before staging, then pushed to `origin/main`.
- `CURRICULUM.md` and `PROJECT_STATE.md` updated with this entry — the last one needed, since the entire original design is now complete.

## Completed — Phase 4, batch 16: DYN/ML/RL/RLEARN exams + Cumulative Final Assessment + Anki export built (2026-08-14, twenty-fourth pass)
- User said "continue, and build the anki thing now" — both confirmed in one message.
- **Anki export built first**: a new build-time Astro endpoint (`src/pages/anki-export.tsv.ts`, a static `.tsv` file generated at build time, not client-side) plus an explainer page (`src/pages/anki.astro`). Deliberately scoped to two card sources requiring zero new authoring: (1) **208 notation cards**, front=symbol/back=meaning, pulled from every module's existing `notation[]` — universal coverage across all 67 modules; (2) **39 exercise recall cards**, front=prompt/back=`fullSolution`, but only for exercises with a populated `reviewCardIds` field. Checking that field's actual usage first revealed it's sparse, historical debris (only ever populated in KIN-02/MATH/OPT/PROB/ML-03, dropped from every module written from ODE onward) — documented honestly on the `/anki` page rather than presented as a deliberate curation signal it isn't. Deliberately did **not** auto-generate cards from Retrieval Questions (no paired answer exists in the source, so a "back" would have to be blank or fabricated). Converts KaTeX's `$...$`/`$$...$$` to Anki's own `[$]...[/$]`/`[$$]...[/$$]` LaTeX tags. Live-verified: correct `Content-Type`/`Content-Disposition` headers, 248-line TSV (1 header + 247 cards) matching the counts displayed on `/anki`.
- **DYN-EXAM**: covers ODE+KIN+DYN together (per the curriculum's own milestone grouping). Connects the LQR value function to a Lyapunov function proof (DYN-04+DYN-06), derives and numerically checks a linearized-pendulum-under-PD closed loop including a deliberately unstable sign-flipped-gain retake (a real, common implementation bug), computes controllability-matrix rank for both a controllable and an uncontrollable system, and derives feedback-linearization's error dynamics as a new instance of DYN-02's point-mass PD form for *any* nonlinear robot (verified against DYN-02's own critical-damping condition). All eigenvalue/rank claims independently verified via NumPy.
- **ML-EXAM**: connects backprop to ML-01's general reverse-mode autodiff and the kernel trick's infinite-feature-space property (conceptual), derives and numerically verifies the push-through identity connecting primal and dual (kernel) ridge regression (checked this isn't already derived in ML-02/ML-05), computes PCA variance-explained via eigendecomposition, and derives+verifies the ensemble variance-reduction formula for both independent and correlated estimators (the correlated case showing variance floors at `rho*sigma^2` rather than going to zero, motivating ensemble diversity, not just size).
- **RL-EXAM**: connects Q-learning's stochastic-approximation convergence proof to value iteration's exact one (RL-02/RL-03), contrasts R-max's binary optimism with UCT's continuous UCB1 bonus, derives and numerically verifies a one-step softmax policy-gradient computation against finite differences, computes concrete UCB1 tree-policy scores, and derives why nonlinear (risk-averse) utility breaks the standard Bellman recursion's linearity assumption, with a concrete equal-expected-value-different-risk policy comparison.
- **RLEARN-EXAM**: contrasts DAgger's live-expert fix for distributional shift with why offline RL can't use the same fix, contrasts residual dynamics learning with domain randomization as different responses to model uncertainty, **numerically verifies RLEARN-05's own claim** that AIRL's discriminator decomposition converges to the advantage function (built and solved two small MDPs via value iteration in Python, confirming `f(s,a,s')` exactly matches `A(s,a)` to machine precision), verifies potential-based reward shaping's `V'=V*-Phi` identity on a fresh MDP, and applies RLEARN-00's Mini Taxonomy to two entirely new robot-learning scenarios not described anywhere in the workbook (a genuine classification-transfer test).
- **Cumulative Final Assessment** (`CUMULATIVE-FINAL`, routed at `/exam/CAP` since it's the closing milestone after CAP, not a separate per-block exam): 8 purely synthesis/reflection exercises spanning the *entire* curriculum, not just one or two blocks — ordering LQR/value-iteration/model-free-learning along a single spectrum, tracing the covector/metric theme from MATH through PLAN/DYN, contrasting SYM-03's combinatorial explosion with CAP-01's convex-decomposition alternative, questioning what OPT-04's convexity guarantee does and doesn't cover in CAP-01's MIQP, a genuine self-check reflection question asking the student to name 4+ blocks CAP-01 draws on and what it reuses from each, tracing the "representation-of-feasible-space" theme from PLAN-02 through SYM-03 to CAP-01, questioning CAP-01's implicit shortest-path utility through RL-07's decision-theory lens, and drawing the structural parallel between PROB-06's single-mode Laplace approximation and CAP-01's own single-grasp-count commitment.
- Full validation: `astro check` (0 errors), site-wide `<ExerciseCard>` cross-reference (357/357 exercises matched two-way), solution id-matching (clean), a new remediation-map integrity check (every `exerciseId`/`moduleId` referenced in every milestone's `remediationMap` resolves to something real — clean), `npm run build` (96 pages, up from 90), site-wide `katex-error` scan (0 matches). Live-verified all 5 new/finished exam pages (`/exam/DYN`, `/exam/ML`, `/exam/RL`, `/exam/RLEARN`, `/exam/CAP`) with 0 KaTeX errors each and a real numeric exercise passed through the grading pipeline on each; confirmed `/curriculum` now shows the milestone-exam link on exactly the 8 blocks the Assessment Schedule specifies (MATH/OPT/PROB/DYN/ML/RL/RLEARN/CAP) and cheat-sheet-only on the other 5 (ODE/KIN/PLAN/MANIP/SYM), generated automatically with no manual wiring.
- **The Assessment Schedule is now fully built out**, matching `CURRICULUM.md`'s original design exactly, except the two explicitly-deferred cumulative-review modules. `CURRICULUM.md` and `PROJECT_STATE.md` updated with this entry. Not yet committed to git.

## Completed — Phase 4, batch 15: all 13 cheat sheets + OPT-EXAM + PROB-EXAM built (2026-08-14, twenty-third pass)
- User said "continue" after batch 14's infrastructure + MATH-EXAM/math cheat sheet. Built all 12 remaining per-block cheat sheets (OPT, PROB, ODE, KIN, DYN, PLAN, MANIP, ML, RL, SYM, RLEARN, CAP), each cross-checked against that block's modules' actual `notation[]`/display-equation content (grepped directly from source, not reconstructed from memory) before writing — every core-equations-table formula traces to a real formula already in the corresponding module. All 67 modules' `cheatsheetLinks` updated to point to their block's sheet (ML-03 keeps its existing pilot cheat sheet link alongside the new one). Fixed the `neural-networks-backprop` cheat sheet's pre-existing dead cross-link to a nonexistent `/cheatsheets/entropy` page, now pointing at `/cheatsheets/prob`.
- Built **OPT-EXAM** and **PROB-EXAM** at the same cumulative-derivation rigor as MATH-EXAM: OPT-EXAM connects Newton/SQP/Gauss-Newton (conceptual), derives two small QPs via KKT (verified by direct grid search since `scipy` isn't installed in `.venv`), computes the SGD convergence bound numerically, and derives KKT stationarity for constrained nonlinear least squares (combining OPT-02's gradient formula with OPT-03's Lagrangian, a genuinely new combination not worked out in either module). PROB-EXAM connects the energy view to the Laplace approximation and to variational inference (conceptual), derives the closed-form KL divergence between two Gaussians (checked first that neither PROB-03 nor PROB-05 already derives this; verified against Monte Carlo estimation), computes a Bayesian Gaussian-fusion measurement update (Bayes' rule + the Gaussian product identity), and applies the Laplace approximation to an energy function distinct from PROB-06's own worked example.
- **Found and fixed a real, previously-undetected build-breaking bug**: `plan.mdx`'s core-equations table had a raw `|` (absolute-value bars, $2|\gamma|$) inside a markdown table cell, which markdown parses as a column delimiter, breaking the entire table and failing the actual MDX build (`Unexpected end of file in expression`) — this passed `astro check` cleanly and only surfaced at `npm run build`, a real gap in the type-checker's coverage worth remembering for any future table containing absolute-value or piecewise-notation LaTeX. A site-wide scan for the same pattern found no other instances. Fixed by using `\vert` instead of a raw `|`.
- Full validation: `astro check` (0 errors), site-wide `<ExerciseCard>` cross-reference extended over all `.mdx` including milestones (318/318 exercises matched two-way), solution id-matching (clean), a new `cheatsheetLinks` integrity check (every module's cheat sheet reference resolves to a real cheat sheet id — clean), `npm run build` (90 pages, up from 76 — caught the `plan.mdx` bug on the first attempt, fixed, rebuilt clean), site-wide `katex-error` scan (0 matches). Live-verified `/exam/OPT` and `/exam/PROB` (0 KaTeX errors each, numeric grading pipeline exercised on both, including a deliberately-wrong answer on PROB-EXAM to confirm the incorrect-answer path also works), `/cheatsheets` (all 14 listed correctly), and `/curriculum` (MATH/OPT/PROB now show both exam and cheat-sheet links; every other block shows cheat-sheet-only, confirming the auto-generated per-block links work with zero additional wiring as more exams are added).
- Not yet committed to git.

## Completed — Phase 4, batch 14: milestone-exam + cheat-sheet infrastructure, MATH-EXAM + MATH cheat sheet built (2026-08-14, twenty-second pass)
- User flagged, after using the finished site, that milestone exams and per-block cheat sheets (both speced in `CURRICULUM.md`/`ARCHITECTURE.md`) were never actually built — only the Phase-3-pilot's ML-03 cheat sheet existed, zero exams. Asked two scoping questions before starting: cheat-sheet granularity (user chose one per block, 13 total) and exam/review batch scope (user chose all 7 block exams + the cumulative final; did not select the 2 cumulative-review modules). User also raised an Anki-integration idea ("fun learning/revising... what do you think?"); gave a recommendation (auto-generate from existing content, not hand-authored cards or a new SRS system) but did not implement, per the "don't implement until the user agrees" rule for exploratory questions.
- **New infrastructure**: a `milestones` content collection (`src/content.config.ts`), reusing the existing `questions`/`solutions` collections for exam exercises (moduleId `{BLOCK}-EXAM`) so exams get the exact same deterministic/symbolic/rubric grading, hints, and Review-with-Claude packet as module exercises, no new grading code. Added an optional `retakeVariantOf` field to the `questions` schema. New `MilestoneLayout.astro` + `/exam/[block]` route (mastery threshold framed as a self-assessment target, not an enforced gate, since there's no server to compute a real score) with a remediation-map table linking each exercise back to the module(s) it draws on.
- **MATH-EXAM built as the reference implementation**: 4 primary + 4 retake exercises, deliberately cumulative (combining 2+ MATH modules, or extending a taught identity beyond what any single module worked out) rather than repeating module-level content — cross-checked against MATH-02B's own worked examples first to confirm the transfer exercises (weighted least squares, general PSD regularizer) weren't accidental duplicates of its ridge-regression derivation. All numeric/derivation content independently verified (eigendecomposition/rank-1-approximation error for two contrasting matrices — one lossless, one lossy — Hessian-as-Jacobian-of-gradient and vector-valued Taylor both via finite-difference checks, both closed-form gradient derivations via direct residual checks).
- **MATH cheat sheet built as the reference implementation** (`src/content/cheatsheets/math.mdx`): Definitions/Core equations/Assumptions/Common mistakes/Decision rule sections condensed from all 8 MATH modules, cross-checked against each module's own `notation[]` for accuracy rather than reconstructed from memory. All 8 modules' `cheatsheetLinks` updated to point back to it (previously empty for every MATH module).
- **Found and fixed another pre-existing gap while wiring this in**: `/cheatsheets` was a dead breadcrumb link (`CheatsheetLayout.astro` linked to it, but no index page ever existed). Built `src/pages/cheatsheets/index.astro`, generated from the `cheatsheets` collection.
- `/curriculum` now shows a "📝 Milestone exam" / "📄 Cheat sheet" link per block section automatically, generated from the `milestones`/`cheatsheets` collections (no hardcoding needed as more are added — the remaining 6 exams and 12 cheat sheets will just appear).
- Full validation: `astro check` (0 errors), site-wide `<ExerciseCard>` cross-reference extended to also scan `src/content/milestones/*.mdx` (302/302 exercises matched two-way), JSON id/hint-count matching, `npm run build` (76 pages, up from 74), site-wide `katex-error` scan (0 matches). Live-verified `/exam/MATH` (remediation-map links correct, both numeric exercises — including the retake — passed through the real grading pipeline end-to-end), `/curriculum`'s new per-block links, `/cheatsheets/math`, and `/cheatsheets` index, all with 0 KaTeX errors.
- `CURRICULUM.md`'s Assessment Schedule section got a new production note; `PROJECT_STATE.md` updated with this entry. Not yet committed to git.

## Completed — Phase 4, batch 13: Block CAP built (curriculum complete) + site navigation wired up (2026-08-13, twenty-first pass)
- User asked to (1) continue and finish Block CAP, (2) wire up the curriculum roadmap/navigation described in `data/curriculum/ARCHITECTURE.md` (previously deferred pending "enough real content"), and (3) add README instructions for manually editing content.
- **CAP-01 (Research Bridge: Convex-Decomposition Manipulation Planning)**: the user's own PhD Year-1 report, needed as this module's primary source, had been inaccessible in every prior batch (flagged as a real gap back in DYN-07); the user added it to `original notes/` mid-session and gave its path. Read the report's ToC and §5.1 (pp.29–35) in full, then built the module around that section's actual content: composite configuration space, transit/transfer paths, grasp/placement spaces, the three-stage convex-decomposition/MIQP/GCS pipeline (including the big-M constraint), and the 3-DOF/4-DOF experimental results table. Explicitly cross-references PLAN-01 (notation is shown to be identical, not just similar), DYN-04, and RLEARN-07 (a newly-authored structural-parallel table between the MIQP and RLEARN-07's TAMP/LGP formulation, honestly noting where the analogy holds and where it doesn't). Cited via the schema's `externalSources` field (distinct from `sources`, reserved for the Toussaint corpus) since this is the user's own document. 5 exercises, all newly authored, numeric ones independently verified. This is the last content module in the entire curriculum.
- **Site navigation/discovery layer wired up**: built `/curriculum` (a build-time-generated roadmap: all modules by block, tier badges, hours, prerequisite links, block-jump nav, client-side filter box — no new dependency, matching `ARCHITECTURE.md`'s own "simple built index" fallback), `/concepts`, `/notation`, and `/sources` index pages (aggregated from existing per-module schema fields), and prev/next navigation on every module page (linear route = block order then module ID). Fixed a real dead link found in the process: the module breadcrumb's block link pointed at a `/course/{block}` route that never existed (404); repointed to `/curriculum#{block}`. Homepage rewritten to link to the new pages instead of listing all 67 modules flatly, and its "not yet wired into navigation" disclaimer removed now that it's true.
- **Found and fixed a real display bug** while wiring CAP-01 in: `ModuleLayout`'s footer unconditionally rendered an empty "Sources cited" section for CAP-01 (which uses only `externalSources`, not `sources`), and never rendered `externalSources` at all, so the report citation was invisible on the page despite being correctly authored in frontmatter. Added a conditional `externalSources` section to the footer; verified live that the citation now renders and that `/sources` picks it up under a new "External sources" heading.
- **README**: added an "Editing content" section (fix text; add/edit math with the multi-line-`$`-closing-fence gotcha called out explicitly; add a `SourceFigure` image or a plain link; add/wire an exercise with the `<ExerciseCard>`-tag gotcha called out explicitly, since that bug class was found and fixed twice this session already; add a whole new module or block; and the exact `astro check`/`build`/katex-scan/cross-reference commands to run afterward, copy-pasteable, tested to actually work as written) so future edits don't require an AI session for small changes.
- Full validation: JSON id/hint-count matching (CAP-01 clean), site-wide `<ExerciseCard>` cross-reference (294/294 exercises matched two-way across all 67 modules, 0 missing, 0 orphaned), `$`-fence scan (clean, no multi-line `\begin{aligned}` blocks in CAP-01). `npx astro check` (0 errors) and `npm run build` (73 pages, up from 68: +CAP-01, +curriculum, +concepts, +notation, +sources) both clean; site-wide `katex-error` dist scan: 0 matches. Live-browser-verified `/curriculum` (including the filter box, tested live: typing "grasp" correctly narrowed to MANIP-01/RLEARN-06/CAP-01 and hid empty block sections), `/concepts`, `/sources` (confirmed the CAP-01 external citation appears), and CAP-01 itself (breadcrumb → `/curriculum#CAP`, prev-nav → RLEARN-08 correctly, 0 KaTeX errors, a numeric exercise passed through the real grading pipeline).
- `data/curriculum/CURRICULUM.md`'s CAP block table and a new production note (covering both CAP-01 and the site-wiring work, and explicitly listing what was deliberately deferred: Pagefind search, print/book export, Anki export, milestone exams) updated. `PROJECT_STATE.md` updated with this entry; "Current phase" now reflects the curriculum as complete.

## Completed — Phase 4, batch 12: Block RLEARN built, all 9 modules (2026-08-13, twentieth pass)
- User said "continue, still keep making sure that all relevant content is covered with definitions and relevant exercises... having some context from the start may be useful," giving two standing instructions applied throughout this batch and going forward: (1) maintain comprehensive, near-total coverage of Toussaint's actual source content in every block, not a representative subset; (2) consider a landscape/taxonomy overview near the start of blocks covering a "zoo" of related methods. Saved as a standing feedback memory (`toussaint_workbook_coverage_and_outlines.md`).
- **RLEARN-00 (Robot Learning: Taxonomy & Landscape)**: new insertion, following the MATH-00 precedent (new ID, no renumbering), built from Toussaint's own "Mini Taxonomy" (§1.2), directly answering instruction (2) above with real source content rather than invented framing — the "what is learned × how is data generated" two-axis taxonomy, a table mapping every cell to its covering module, and the "robot learning as shortcuts to the model-based control stack" framing.
- **RLEARN-01 (Dynamics Learning)** through **RLEARN-07 (TAMP & Logic-Geometric Programs)**: parameter estimation/regression/residual dynamics, autoregressive/recurrent/state-space models; behavior cloning, DAgger, distributional shift (Ross/Gordon/Bagnell compound-error bound stated and explained, not re-derived), GAN/VAE/diffusion-policy IL instantiations; reward shaping (Ng/Harada/Russell, numerically verified via random-MDP value iteration) and structured exploration noise; offline RL, domain randomization, privileged-teacher/student sim2real; Max-Margin and Max-Entropy IRL, Adversarial IRL (its discriminator decomposition explicitly connected, as a newly-authored synthesis, to RLEARN-03's reward-shaping formula and RL-04's Bellman equation, showing $f_{\theta,\phi}\to A(s,a)$), Bradley-Terry preference learning; model-based and simulation-based grasp labeling (both framed as privileged-teacher instances); the full Logic-Geometric Program formulation, TAMP's task/motion/force abstraction levels, Deep Visual Constraints and Deep Visual Reasoning (the latter connected term-by-term to a language model's next-token prediction), flagged by the curriculum as "the module most directly aligned with your research."
- **RLEARN-08 (Multi-Robot Learning, light touch)**: Tier-3 survey depth matching the ML-07/SYM-01/MANIP precedent — Deep Sets, GNN aggregate/update, Markov games as the multi-robot generalization of RL-01's MDP, MARL's non-stationarity and scalability challenges, CTDE.
- Heavy cross-referencing discipline throughout, since RLEARN sits on top of nearly the entire curriculum (RL, ML, DYN, PROB, MANIP-01, SYM-03, OPT-06) — see `CURRICULUM.md`'s production note for the full list of which modules cross-reference which.
- **Found and fixed a real schema bug**: a stray empty `notation: [{}]` placeholder (leftover from drafting) in both RLEARN-00's and RLEARN-06's frontmatter, caught by `npx astro check` (`notation.0.symbol: Required`), not by any of the custom validation scripts. Fixed by giving RLEARN-00 a real notation entry and removing the empty key from RLEARN-06 (which didn't otherwise use the field).
- All numeric exercises independently verified before writing (Deep Sets aggregation, reward-shaping fixed point, Bradley-Terry probability, ACRONYM grasp counts, state-space-size logs).
- Full validation sweep across RLEARN-05 through RLEARN-08: JSON id-matching + 3-hint-tuple check (clean), site-wide `<ExerciseCard>` cross-reference (289/289 exercises matched two-way across all 66 modules, 0 missing, 0 orphaned), `$`-fence scan (no multi-line `\begin{aligned}` blocks in this block, so the earlier KaTeX bug class doesn't apply here).
- `npx astro check` (0 errors, after the notation-field fix) and `npm run build` (68 pages) both clean; site-wide `katex-error` dist scan: 0 matches. Live-browser-verified RLEARN-00, RLEARN-07, and RLEARN-08 (0 `.katex-error` elements on any), including RLEARN-08's Deep Sets numeric exercise passed through the real client-side grading pipeline end-to-end (`✓ Correct (within tolerance ±0.001)`).
- `CURRICULUM.md`'s RLEARN block table (new RLEARN-00 row, hours updated to match actually-written frontmatter) and a new production note updated; block hours grew from the original ~20–24 estimate to ~25.5 across 9 modules. `PROJECT_STATE.md` updated with this entry.

## Known uncertainties
- ~598 pages of the 4 large lecture PDFs were not visually spot-checked as rendered images: deferred to per-module authoring time, per `AUDIT_REPORT.md` §6. (In practice, every figure actually embedded via `SourceFigure` across the finished curriculum was visually confirmed at authoring time; this note applies only to pages that were never cited by any module.)
- Cross-reference numbers (`\ref`/`\eqref`) extract as `??` in PDF text: resolve against the live PDF when a module cites them (not needed for ML-03 or KIN-02, both of which drew from sources without such references, or from HTML-sourced notes).
- ~~Review-with-Claude clipboard behavior and progress-import file upload: unverified in a real browser.~~ **Resolved 2026-08-13 (batch 14)**: both verified live in-browser via direct calls to the underlying `store.ts`/`ExerciseCard` logic — the clipboard packet matches the spec'd format exactly (module/exercise ID, question, answer, rubric, diagnose-first-error instruction), and export→wipe→import round-trips progress data correctly (confirmed via a full IndexedDB delete followed by re-import from the exported JSON).

## Validation status
- Extraction pipeline: clean, 0 errors, 13/13 sources.
- Astro type check (`npx astro check`): 0 errors.
- Production build: succeeds, 4 pages (index, ML-03, KIN-02, 1 cheatsheet).
- Manual + automated interaction testing across both modules: numeric checker ✓, symbolic checker (live SymPy round-trip, including alternate algebraic forms) ✓, rubric/attempt flow ✓, IndexedDB progress ✓, KaTeX rendering (correct `katex-sizing` classes and 0.7em scaling after the dedup fix) ✓, cheat sheet page ✓, homepage ✓, progress export ✓. Clipboard copy and progress import: not independently exercised in the automated browser tool; user should verify both themselves.
- Git: public repo at `https://github.com/julialopezgomez/toussaint-workbook`, source PDFs and extracted text excluded throughout.
- Production build after batch 1: succeeds, 9 pages, 0 type errors.

## Next exact action
Get user confirmation on batch 2's scope (see "Pending" above), then produce it, validate it, and report before starting the next one.
