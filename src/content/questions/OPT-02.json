{
  "moduleId": "OPT-02",
  "exercises": [
    {
      "id": "OPT-02-ex1-why-approximate-hessian",
      "objective": "Explain why approximating the Hessian is often necessary in practice.",
      "provenance": "newly-authored",
      "difficulty": "foundation",
      "estimatedMinutes": 6,
      "prerequisites": [],
      "answerType": "short-text",
      "prompt": "Newton's method (OPT-01) needs the full Hessian $H(x) \\in \\mathbb{R}^{n\\times n}$ at every step. For $n$ in the thousands or millions (e.g. a neural network's parameters, or a fine-grained robot trajectory), explain why computing and storing (let alone inverting) the exact Hessian at every step becomes impractical, and what general strategy the methods in this module use instead.",
      "hints": [
        "The Hessian has $n^2$ entries, and computing each one generally requires differentiating the gradient again; storing it needs $O(n^2)$ memory, and inverting/solving with it costs $O(n^3)$ in general, all of which quickly become infeasible as $n$ grows.",
        "The general strategy this module covers: approximate the Hessian (or its inverse, or its action on a vector) using cheaper information, either structure specific to the problem (Gauss-Newton), a running history of gradient observations (quasi-Newton/BFGS), or an iterative method that never forms the Hessian explicitly at all (conjugate gradient).",
        "This is the same theme as MATH-05's covariant gradient: correct the plain gradient direction using curvature information, but here the emphasis shifts to how to get a USABLE approximation of that curvature information cheaply, rather than assuming you already have it."
      ],
      "reviewCardIds": ["RC-OPT-02-01"]
    },
    {
      "id": "OPT-02-ex2-gauss-newton-derivation",
      "objective": "Derive the Gauss-Newton Hessian approximation from the exact Hessian of a least-squares objective.",
      "provenance": "newly-authored",
      "difficulty": "core",
      "estimatedMinutes": 18,
      "prerequisites": ["OPT-02-ex1-why-approximate-hessian"],
      "answerType": "derivation",
      "prompt": "For a least-squares objective $f(x) = \\tfrac12 \\|r(x)\\|^2 = \\tfrac12 r(x)^\\top r(x)$, where $r:\\mathbb{R}^n\\to\\mathbb{R}^m$ is a (possibly nonlinear) residual with Jacobian $J(x) = \\partial r/\\partial x$: (a) derive $\\nabla f(x) = J(x)^\\top r(x)$, and (b) show that the exact Hessian has the form $H(x) = J(x)^\\top J(x) + \\sum_i r_i(x)\\nabla^2 r_i(x)$, and explain which term the **Gauss-Newton approximation** $H(x) \\approx J(x)^\\top J(x)$ drops, and why that's often a reasonable approximation.",
      "hints": [
        "For the gradient: use the chain rule on $f(x) = \\tfrac12\\sum_i r_i(x)^2$. $\\partial f/\\partial x_k = \\sum_i r_i(x) \\partial r_i/\\partial x_k$, which in matrix form is exactly $J(x)^\\top r(x)$ (each column of $J^\\top$, i.e. row of $J$, dotted with $r$).",
        "For the Hessian, differentiate $\\nabla f(x) = J(x)^\\top r(x)$ once more w.r.t. $x$, using the product rule: one term comes from differentiating $J(x)^\\top$ (holding $r$ fixed), giving $J^\\top J$; the other comes from differentiating $r(x)$ (holding $J^\\top$ fixed), giving a term involving each residual $r_i$ weighted by its own second derivative $\\nabla^2 r_i(x)$.",
          "The dropped term $\\sum_i r_i(x)\\nabla^2 r_i(x)$ requires the second derivatives of the residuals AND is weighted by how large the residuals themselves are. Near a good fit (small residuals $r_i(x) \\approx 0$), this term is small regardless of how curved each $r_i$ is, so dropping it (the Gauss-Newton approximation) is reasonable precisely when the fit is already decent, which is usually true near convergence, exactly when you need the Hessian approximation to be good."
      ],
      "reviewCardIds": ["RC-OPT-02-02"]
    },
    {
      "id": "OPT-02-ex3-numeric",
      "objective": "Compute a Gauss-Newton step for a small least-squares problem.",
      "provenance": "newly-authored",
      "difficulty": "core",
      "estimatedMinutes": 15,
      "prerequisites": ["OPT-02-ex2-gauss-newton-derivation"],
      "answerType": "numeric",
      "prompt": "Let $r(x) = \\begin{pmatrix}x_1 - 1\\\\ x_2 - 2\\\\ x_1+x_2-2\\end{pmatrix}$ (a linear residual, so $J$ is constant), starting at $x_0=\\begin{pmatrix}0\\\\0\\end{pmatrix}$. Compute the Gauss-Newton step $\\delta = -(J^\\top J)^{-1}J^\\top r(x_0)$, and report the **second component** of $x_1=x_0+\\delta$. Enter a decimal to 3 decimal places.",
      "hints": [
        "$J = \\begin{pmatrix}1&0\\\\0&1\\\\1&1\\end{pmatrix}$ (each row is $\\partial r_i/\\partial x$). $r(x_0) = \\begin{pmatrix}-1\\\\-2\\\\-2\\end{pmatrix}$.",
        "$J^\\top J = \\begin{pmatrix}2&1\\\\1&2\\end{pmatrix}$, $J^\\top r(x_0) = \\begin{pmatrix}1(-1)+0(-2)+1(-2)\\\\0(-1)+1(-2)+1(-2)\\end{pmatrix} = \\begin{pmatrix}-3\\\\-4\\end{pmatrix}$.",
        "Solve $\\begin{pmatrix}2&1\\\\1&2\\end{pmatrix}\\delta = \\begin{pmatrix}3\\\\4\\end{pmatrix}$ (note the sign flip from $-J^\\top r$): $\\delta \\approx \\begin{pmatrix}0.667\\\\1.667\\end{pmatrix}$. Since the residual is linear here, Gauss-Newton is exact (not approximate), so $x_1$ is the true least-squares solution: second component $\\approx 1.667$."
      ],
      "reviewCardIds": []
    },
    {
      "id": "OPT-02-ex4-path-opt-structure",
      "objective": "Identify the sum-of-squares structure and Jacobian sparsity in a small robotics path-optimization problem.",
      "provenance": "source-adapted",
      "sourceRef": { "sourceId": "lecture-optimization", "page": 16, "originalNumber": "§1.4:6, robotics example" },
      "difficulty": "core",
      "estimatedMinutes": 15,
      "prerequisites": ["OPT-02-ex3-numeric"],
      "answerType": "derivation",
      "prompt": "Consider a scalar (1D), 2-timestep path $x=(x_1,x_2)$, with boundary $x_0=0$, $x_{-1}=0$ given, minimizing $\\sum_{t=1}^2(x_t+x_{t-2}-2x_{t-1})^2 + (x_2-5)^2$ (a smoothness cost plus an endpoint target of 5). Write out the feature vector $\\phi(x)=(\\phi_1,\\phi_2,\\phi_3)$ explicitly in terms of $x_1,x_2$, then write its Jacobian $J=\\partial\\phi/\\partial x \\in\\mathbb{R}^{3\\times2}$, and comment on which entries are zero and why.",
      "hints": [
        "$\\phi_1 = x_1+x_{-1}-2x_0 = x_1$ (since $x_{-1}=x_0=0$). $\\phi_2 = x_2+x_0-2x_1 = x_2-2x_1$. $\\phi_3 = x_2-5$ (the endpoint target term).",
        "Differentiate each $\\phi_i$ w.r.t. $x_1,x_2$: $\\partial\\phi_1/\\partial x_1=1,\\partial\\phi_1/\\partial x_2=0$; $\\partial\\phi_2/\\partial x_1=-2,\\partial\\phi_2/\\partial x_2=1$; $\\partial\\phi_3/\\partial x_1=0,\\partial\\phi_3/\\partial x_2=1$.",
        "$J=\\begin{pmatrix}1&0\\\\-2&1\\\\0&1\\end{pmatrix}$. Every zero entry reflects that a given smoothness term only depends on nearby timesteps (here $\\phi_1$ doesn't involve $x_2$ at all, and $\\phi_3$ doesn't involve $x_1$): in a longer path this banded/local dependency structure is exactly what keeps $J$ (and hence $J^\\top J$) sparse, making Gauss-Newton tractable even for very long trajectories."
      ],
      "reviewCardIds": []
    },
    {
      "id": "OPT-02-ex5-secant-check",
      "objective": "Verify a candidate Hessian approximation against the secant equation, and explain why it doesn't pin down a unique matrix.",
      "provenance": "source-adapted",
      "sourceRef": { "sourceId": "lecture-optimization", "page": 17, "originalNumber": "§1.4:8" },
      "difficulty": "core",
      "estimatedMinutes": 10,
      "prerequisites": ["OPT-02-ex1-why-approximate-hessian"],
      "answerType": "short-text",
      "prompt": "Two gradient evaluations give $\\delta=(1,0)$, $y=(2,1)$. Does $\\hat H=\\begin{pmatrix}2&1\\\\1&3\\end{pmatrix}$ satisfy the secant equation $\\hat H\\delta=y$? Then explain why a single $(\\delta,y)$ pair, in $n=2$ dimensions, does not uniquely determine $\\hat H$ (how many free parameters does a symmetric $2\\times2$ matrix have, versus how many scalar equations does $\\hat H\\delta=y$ provide?), motivating why BFGS needs a specific update *rule*, not just any matrix satisfying the secant equation.",
      "hints": [
        "Compute $\\hat H\\delta = \\begin{pmatrix}2&1\\\\1&3\\end{pmatrix}\\begin{pmatrix}1\\\\0\\end{pmatrix} = \\begin{pmatrix}2\\\\1\\end{pmatrix}$, which does equal $y=(2,1)$. So yes, it satisfies the secant equation.",
        "A symmetric $2\\times2$ matrix has 3 free parameters (2 diagonal, 1 off-diagonal). The secant equation $\\hat H\\delta=y$ is a vector equation in $\\mathbb{R}^2$, i.e. only 2 scalar equations.",
        "3 unknowns, 2 equations: the secant equation alone leaves a 1-parameter family of valid symmetric matrices, it's underdetermined. This is exactly why BFGS specifies a particular *update rule* (the rank-2 correction from the previous estimate), rather than just solving the secant equation directly, it picks out one specific, well-behaved member of that family, incorporating the previous estimate's information rather than discarding it."
      ],
      "reviewCardIds": []
    },
    {
      "id": "OPT-02-ex6-cg-quadratic",
      "objective": "Numerically verify conjugacy of two successive conjugate-gradient search directions on a quadratic.",
      "provenance": "newly-authored",
      "difficulty": "challenge",
      "estimatedMinutes": 15,
      "prerequisites": ["OPT-02-ex5-secant-check"],
      "answerType": "numeric",
      "prompt": "For $f(x)=x^\\top Ax$ with $A=\\text{diag}(1,4)$, one run of the conjugate gradient algorithm from $x_0=(1,1)$ produces the first two search directions $\\delta_0=(-2,-8)$ and $\\delta_1\\approx(-1.545,\\,0.097)$. Compute $\\delta_0^\\top A\\delta_1$ (the conjugacy check) and confirm it is approximately zero. Enter your result to 2 decimal places.",
      "hints": [
        "$A\\delta_1 = \\text{diag}(1,4)\\begin{pmatrix}-1.545\\\\0.097\\end{pmatrix} = \\begin{pmatrix}-1.545\\\\0.388\\end{pmatrix}$ (multiply each entry by its diagonal weight).",
        "$\\delta_0^\\top(A\\delta_1) = (-2)(-1.545) + (-8)(0.388)$.",
        "$= 3.09 - 3.104 \\approx -0.01 \\approx 0$ (the small residual is just rounding in the given $\\delta_1$; algebraically this is exactly zero). This confirms the Polak-Ribière construction really did produce a direction $H$-conjugate to the previous one, exactly as the CG algorithm guarantees for a quadratic objective."
      ],
      "reviewCardIds": []
    }
  ]
}
