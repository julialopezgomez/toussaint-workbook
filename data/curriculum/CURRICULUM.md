# Phase 2 — Curriculum Design

Generated 2026-08-12. Derived from `data/source-manifest/AUDIT_REPORT.md` (source coverage) and its §6b priority tiers (confirmed by user, see `docs/decisions/0001-corpus-update-and-priorities.md`). This is the **planning artifact** for Gate 2 — it fixes IDs, sequencing, sources, and scope. Full lesson content (worked examples, hints, exercise text) is authored later, per module, in Phase 3 (pilot) and Phase 4 (batches).

## How to read this document

13 blocks, ~63 modules, one linear route. Each block table has columns: **ID** (stable, `{BLOCK}-{NN}`) · **Title** · **Source(s) & pages** (`p` = PDF page, `[html]` = also has a cleaner LaTeX source per the audit's §5b) · **Objective** (compressed to 1 line — full multi-part objectives are written when the module is authored) · **Prereqs** (module IDs) · **Hrs** (estimated study hours, calibrated to your stated rustiness/strengths from Phase 0 and the Edinburgh-teaching exception for core kinematics) · **Tier** (from §6b: 1=main route core, 2=main route but sequenced for dependency reasons, 3=optional branch, 4=reference-only branch).

**Mastery criteria policy** (stated once, not repeated 63 times): a module is "mastered" when you've completed its Core-tier exercises correctly (self-checked or Claude-reviewed per rubric) and can answer its retrieval questions without notes. Modules explicitly marked "derivation-heavy" additionally require completing at least one Challenge-tier derivation. Exact per-module mastery checklists are written at authoring time.

**Exercises used/created**: per the audit, **zero solutions exist anywhere in the source corpus**, so every exercise's rubric/solution is newly authored regardless of whether the question itself is source-adapted or new. Source-adapted exercises preserve original numbering in their provenance metadata (e.g. `lecture-maths-ex4-iii`).

**Obsidian connections**: your vault is mostly per-paper literature notes, not atomic concept notes, so most modules won't have a natural "connect to your notes" target. I've flagged the clear exceptions below at block level; the rest get resolved at authoring time by checking `Literature Notes/Definitions Database.md` and `References Database.md` for term overlap, per the project's original knowledge-mapping process — I won't force weak matches.

---

## Block Summary

| Block | Title | Tier | Modules | Est. hours | Key prereq blocks |
|---|---|---|---|---|---|
| MATH | Math Foundations Refresher | 1 | 8 | 31–34 | — |
| OPT | Optimization | 1 | 6 | 28–29 | MATH |
| PROB | Probability & Information Theory | 2 (sequenced early) | 6 | 18 | MATH |
| ODE | ODEs & Dynamical Systems Primer | 1 | 3 | 11.5 | MATH |
| KIN | Rigid-Body Rotations & Kinematics | 1 (revision-paced except quaternions) | 3 | 8 | MATH |
| DYN | Robot Dynamics & Control | 1 | 7 | 18–22 | MATH, ODE, KIN, OPT |
| PLAN | Path & Motion Planning | 1 | 4 | 9–11 | KIN, OPT |
| MANIP | Manipulation & Legged Locomotion Foundations | 2 | 2 | 4–5 | KIN, DYN |
| ML | ML & Autodiff Bridge | 1 | 7 | 17–20 | MATH, OPT, PROB |
| RL | Reinforcement Learning Foundations | 1 | 7 | 16–19 | PROB, ML |
| SYM | Symbolic Foundations for TAMP | 1/2 | 4 | 8–10 | — (light: PROB helps K4) |
| REV1 | Cumulative Review I: Foundations Through Symbolic Reasoning | 1 | 1 | 1.5 | MATH, OPT, PROB, ODE, KIN, DYN, PLAN, MANIP, ML, RL, SYM |
| RLEARN | Robot Learning (capstone-adjacent) | 1 | 9 | ~25.5 | DYN, PLAN, ML, RL, SYM, MANIP |
| REV2 | Cumulative Review II: Robot Learning | 1 | 1 | 1 | RLEARN |
| CAP | Capstone: Research Bridge | 1 | 1 + final assessment | 6–8 | all of the above |

**Total: ~69 modules, ~163–197 estimated study hours** (roughly 4–5 months at 8–10 hrs/week, or however you choose to pace it — no deadline was set, so treat this purely as a sizing signal). Production happens in batches (Phase 4); you'll never face this whole list as a wall of unstarted content. All modules are now built (Phase 4 complete as of batch 17).

**Optional/reference-only branches** (reachable via links from the main route, not on it): LM Reasoning/RLHF & Explainable AI (off RL), SLAM & State Estimation (off PLAN), general Search/CSP beyond the MCTS prerequisite (off SYM). These get compact single-module treatments, not full sub-curricula.

---

## Block MATH — Math Foundations Refresher

Rationale: your Phase 0 background check flagged calculus/linear algebra as rusty, needing genuine step-by-step re-derivation rather than notation review. This block front-loads that, concrete-before-abstract (SVD gets an intuition-first pass via the short note before the fuller treatment).

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| MATH-00 | Taylor Expansion From Scratch | `lecture-maths` p14 (brief mention only) | Derive first- and second-order Taylor approximations from the definition of the derivative, not just recall the formulas | — | 2 | 1 |
| MATH-01 | Functions, Derivatives & the Chain Rule | `lecture-maths` p5–11, p19 (§2.1–2.2, §2.6.1) | Rebuild fluency computing partial/total derivatives and chain rules by hand, incl. computation-graph view, notation conventions, and formal continuity/differentiability/smoothness definitions | MATH-00 | 4 | 1 |
| MATH-02 | Gradients, Jacobians, Hessians & Taylor Expansion | `lecture-maths` p12–15, p18–19 (§2.3, §2.5, §2.7.2) | Compute gradients/Jacobians/Hessians from formal definitions (2.3–2.5); interpret Hessian eigenvalues as curvature/convexity/saddle classification; numerically verify via the finite-difference Jacobian check (Algorithm 1) | MATH-01 | 4 | 1 |
| MATH-02B | Matrix Calculus: Identities, Warm-Ups & Applied Examples | `lecture-maths` p15–18 (§2.4–2.4.3) | Solve matrix equations, derive ridge regression from scratch, apply the core matrix-derivative identities, and work through the GP-regression and logistic-regression worked examples, plus a Gauss-Newton robotics IK capstone | MATH-02 | 5 | 1 |
| MATH-03 | Vector Spaces, Dual Spaces & Coordinate Transformations | `lecture-maths` p21–31 (§3.1–3.3, excl. scalar product) | Reconstruct vector-space/basis/dual-space concepts (Def. 3.1–3.9) from formal axioms, connect the linear-function taxonomy (1-forms/2-forms) to gradients, and derive change-of-basis and the matrix similarity transform | MATH-01 | 5 | 1 |
| MATH-03B | Scalar Products, Metric Tensors & Orthonormal Bases | `lecture-maths` p28–31 (§3.3) | State the formal scalar-product (Def. 3.10) and general positive-(semi-)definite matrix definitions with Sylvester's criterion; compute with a general metric tensor; derive the orthogonal-projection formula | MATH-03 | 4 | 1 |
| MATH-04 | SVD, Eigendecomposition & the Power Method | `svd` [html] (intuition-first) → `lecture-maths` p31–38 (§3.4–3.7) | Derive and interpret the SVD theorem, pseudo-inverse, and covariance/PCA connection; state the eigendecomposition definition and characteristic polynomial; prove core eigenvalue facts (PSD equivalence, orthogonality, shift/power invariances); derive the power method (incl. smallest-eigenvalue variant) | MATH-03B | 7 | 1 |
| MATH-05 | Covariant Gradient & Steepest Descent | `lecture-maths` p39–42 (§3.9) | Distinguish contravariant/covariant vectors (incl. the formal differential-as-1-form, Def. 3.19); derive the steepest-descent direction under a metric | MATH-04 | 2 | 1 |

**Milestone**: Math Foundations Exam (see Assessment Schedule).

**Production note (Phase 4, post-batch-1)**: MATH-00 (Taylor Expansion From Scratch) was added after batch 1 shipped, once the user flagged that Taylor expansion was used as a derivation tool in both MATH-01 and MATH-02 without ever being taught from scratch. Inserted as "00" rather than renumbering MATH-01 through MATH-05, to avoid churning already-published IDs/cross-references for a late addition.

**Production note (Phase 4, batch 1)**: `lecture-maths` Exercises 4 and 5 (§2.7–2.8, "Backprop in a Neural Net") were originally scoped as MATH-02's own exercises but ended up used in [ML-03](/course/ML/ML-03) instead, since they fit thematically better alongside neural-network content and ML-03 was built first as a pilot. MATH-02 as actually authored uses newly-written exercises instead, covering the same gradient/Jacobian/Hessian mechanics without duplicating ML-03's material.

**Production note (Phase 4, comprehensive MATH revision, 2026-08-12)**: The user flagged that batch-1 MATH content (and OPT/PROB, addressed in a subsequent pass) had been verified-correct but too thin: missing formal definitions actually present in the source (e.g. positive semi-definite/definite matrices, which the source only implicitly requires via Definition 3.10's "positive definite 2-form" phrasing, never spelled out as its own box), missing the source's actual figures, and missing large chunks of source content and exercises (the entire "Derivatives with Matrices" section §2.4, the eigendecomposition/power-method material §3.7, and roughly 15 previously-unused source exercises). Block MATH was expanded from 6 to 8 modules to accommodate this at a digestible pace: MATH-02B (matrix calculus) split out of MATH-02, and MATH-03B (scalar products/metric tensor/PSD-PD) split out of MATH-03; MATH-04 was substantially expanded in place (SVD, pseudo-inverse, PCA connection, eigendecomposition, and both power-method variants) rather than split further, to avoid exceeding the two-module expansion already communicated to the user. Two real figures from the source PDF (the chain-rule computation-graph diagram and the SVD project-scale-unproject illustration) are now embedded directly via `SourceFigure.astro`, made possible by the repository being reverted to private (see `docs/decisions/0003-public-repo.md`). Total MATH block time grew from ~15 hrs to ~33 hrs as a direct consequence of genuinely covering the source's actual depth rather than a paraphrased subset.

---

## Block OPT — Optimization

Rationale: Tier 1, directly matches your report's optimal-control formalism (Eq. 2.2) and current manipulation-planner work. `lecture-optimization` is the primary source (superset of `lecture-maths` Ch.4); its own exercises are used, cross-referenced against `lecture-maths`' unique ones.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| OPT-01 | Unconstrained Optimization: Line Search & Newton | `lecture-optimization` p5–14 (§1.1-1.3); `lecture-maths` p47–58 | Derive downhill-algorithm desiderata, backtracking line search + both Wolfe conditions, steepest descent under a metric, and Newton's method (root-finding → nD), incl. the non-PD fallback | MATH-02 | 5 | 1 |
| OPT-02 | Gauss-Newton, Quasi-Newton & Conjugate Gradient | `lecture-optimization` p14–20 (§1.4) | Derive Gauss-Newton (incl. PSD/Riemannian-metric interpretation) for least-squares; derive the BFGS secant equation/update and the CG algorithm | OPT-01 | 4.5 | 1 |
| OPT-03 | Constrained Optimization: KKT & the Lagrangian | `lecture-optimization` p21–23, p30–34 (§2.1, §2.4); `lecture-maths` p58–67 | State the full KKT theorem (4 conditions); derive the Lagrangian/KKT relations; state weak duality and Slater's strong-duality condition | OPT-01 | 5 | 1 |
| OPT-04 | Convex Problems, LP/QP, SQP & Log Barriers | `lecture-optimization` p23–30, p35–44 (§2.2-2.3, §2.5) | State convex/quasiconvex/unimodal definitions; derive the log-barrier method (incl. central path/relaxed-KKT) and the augmented Lagrangian; recognize/formulate LP/QP/SQP | OPT-03 | 5.5 | 1 |
| OPT-05 | Stochastic & Blackbox Optimization | `lecture-optimization` p56–64 (§3.1-3.2) | State the SGD algorithms and derive its Lipschitz-based convergence bound; state RMSprop/Adagrad/momentum/Adam and the formal No-Free-Lunch theorem | OPT-02 | 4.5 | 1 |
| OPT-06 | Bayesian Optimization & Bandits | `lecture-optimization` p71–77 (§3.3); `lecture-ai` p45–51 (§1.4 Bandits/UCB1) | State the bandit problem, UCB1 and its regret bound; state the GP definition/posterior and the acquisition functions (MPI/EI/UCB) | OPT-05, PROB-01, PROB-05 | 4 | 2 (bandits) / 1 (BayesOpt) |

**Milestone**: Optimization Exam. **Obsidian**: `antonova_Rethinking_2023` (differentiable optimization) and `amos_Differentiable_2019` (differentiable MPC) are high-confidence connections for OPT-04/OPT-01 respectively — flagged for linking at authoring time.

**Production note (Phase 4, batch 2)**: OPT-01 through OPT-05 built; OPT-06 (Bayesian Optimization & Bandits) deliberately deferred, since it depends on PROB-01 (Probability Foundations), which doesn't exist yet. **Update (batch 3): Block PROB is now built, so OPT-06 is unblocked** — a good candidate for the start of batch 4.

**Production note (Phase 4, comprehensive OPT revision, 2026-08-12)**: Following the same user-flagged gap as Block MATH (batch-1 content verified-correct but too thin, most notably missing Newton's method's actual derivation, explicitly named by the user), OPT-01 through OPT-05 were substantially expanded in place (not split into new modules, unlike MATH) after re-reading `lecture-optimization.pdf` pages 5-64 and 71-77, and `lecture-ai.pdf` pages 45-51, as rendered images. Newton's method is now derived properly from root-finding first (matching the source's own pedagogical order), with the non-positive-definite-Hessian fallback and Levenberg-Marquardt damping added (previously entirely absent); the full KKT theorem (all 4 conditions), weak/strong duality and Slater's condition, the log-barrier method's central-path/relaxed-KKT connection, the augmented Lagrangian's update rule, the BFGS secant equation and CG algorithm, and the SGD convergence theorem (with its derivation) were all added, none of which existed in the batch-2 version. **OPT-06 (Bayesian Optimization & Bandits) was built for the first time in this pass** (never actually produced in batch 3/4 despite being unblocked), with the full UCB1/GP/acquisition-function formalism from the source. Total OPT block time grew from ~15 hrs to ~29 hrs.

---

## Block PROB — Probability & Information Theory

Rationale: Tier 2 but sequenced early — OPT-06, DYN, ML, and RL all depend on it. `lecture-ai` §1.2 is now the primary source (most complete in corpus, confirmed in the audit); the short notes give first-principles motivation.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| PROB-01 | Probability Foundations | `lecture-ai` p10–17 (§1.2 through Multivariate Distributions) | State the formal probability-measure/random-variable definitions and the joint/marginal/conditional/Bayes' rule box; apply Bayes' rule and marginalization | MATH-01 | 4.5 | 2 |
| PROB-02 | Monte Carlo & Sampling Methods | `lecture-ai` p18–19, p24–26 | State the Monte Carlo estimator formally; derive and implement rejection/importance sampling; apply the particle-approximation representation | PROB-01 | 3 | 2 |
| PROB-03 | Entropy, Cross-Entropy & KL Divergence | `entropy` [html] (primary); `lecture-ai` p12–14 | State the neg-log-likelihood/surprise/optimal-code-length framing; derive entropy as expected surprise; connect cross-entropy to ML loss functions and KL to model error | PROB-01 | 2.5 | 2 |
| PROB-04 | Energy-Based Views & the Boltzmann Distribution | `energy` [html] | Derive the energy/probability correspondence from multiplicative-probability axioms | PROB-03 | 1.5 | 2 |
| PROB-05 | Gaussians: Definition, Identities & Manipulation | `gaussians` [html] (reference-style); `lecture-ai` p17–18 | State the formal Gaussian density (1D/nD, moment/canonical form); derive and apply Gaussian product/marginal/conditional identities — this becomes a cheat-sheet-anchored module | PROB-01 | 4 | 2 |
| PROB-06 | Laplace Approximation, Variational Inference & Fisher Information | `lecture-maths` p92–96 (§5.4-5.6, Def. 5.10-5.11) | Derive the Laplace approximation (Def. 5.10) as a 2nd-order Taylor expansion of log-probability, incl. the source's robot-path motivating example; state variational inference (Def. 5.11) | PROB-05, MATH-02 | 2.5 | 2 |

**Milestone**: Probability Exam.

**Production note (Phase 4, comprehensive PROB revision, 2026-08-12)**: Following the same user-flagged gap as Blocks MATH and OPT, and explicitly naming the Gaussian distribution ("you don't even give me a definition") as an example, Block PROB was re-derived from `lecture-ai.pdf` pages 10-26 and `lecture-maths.pdf` pages 92-96 as rendered images, plus the already-fetched `gaussians`/`entropy`/`energy` HTML notes. **PROB-05's missing formal Gaussian density definition (1D and $n$D, moment and canonical form) — the exact gap the user named — is now added directly**, along with the joint-Gaussian conditional formula (previously described only qualitatively). PROB-01 gained the formal probability-measure axioms, random-variable definition, and the named posterior/likelihood/prior/evidence terms in Bayes' rule (source's own standard distribution families were deliberately left out, the source itself marks them "not relevant for the lecture/exam"). PROB-02 gained the formal Monte Carlo estimator definition and an entirely new particle-approximation section (weighted particles, connected to particle filters and sample-based planning, directly relevant to the user's research). PROB-03 gained the neg-log-likelihood/surprise/optimal-code-length framing with a worked coin-toss example. PROB-06 gained the source's actual Definition 5.10 (Laplace approximation, including its motivating robot-path/collision-detection example) and Definition 5.11 (variational inference); its Fisher information section honestly reflects that the source itself leaves that topic as an unfinished "TODO." PROB-04 was reviewed and found already accurate to its source; left largely unchanged. Total PROB block time grew from ~12–15 hrs to ~18 hrs.

---

## Block ODE — ODEs & Dynamical Systems Primer

Rationale: the one confirmed gap — no source teaches this from first principles, and it's your stated weakest area, directly gating Block DYN. **Newly authored + externally sourced** (candidate: a standard ODE/dynamical-systems primer, to be finalized at authoring time — open question, see end of document).

| ID | Title | Source | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| ODE-01 | First-Order ODEs: What They Are & How to Solve Them by Hand | External + newly authored | State the formal definitions of an ODE/order/solution/IVP; solve separable and linear first-order ODEs manually; state the Picard-Lindelöf existence/uniqueness condition | MATH-02 | 4 | 1 |
| ODE-02 | Linear Systems of ODEs, Eigenvalues & Stability | External + newly authored | Derive the eigenvector solution ansatz for linear systems (ties directly to MATH-04); classify stability from eigenvalues; connect to linearized-dynamics analysis | ODE-01, MATH-04 | 4 | 1 |
| ODE-03 | Numerical Integration: Euler & RK4 | External + newly authored | Derive forward Euler from a Taylor expansion; state and apply the RK4 algorithm; compare their accuracy/cost tradeoff by hand | ODE-02 | 3.5 | 1 |

**Milestone**: folded into the DYN block's milestone rather than a standalone exam (ODE is a short, tightly-scoped primer, not a full course).

**Production note (Phase 4, batch 4, 2026-08-12)**: Built with the same rigor as the comprehensively-revised MATH/OPT/PROB blocks (formal definitions, independently-verified worked examples — SymPy for ODE-01/02's symbolic checks, NumPy/from-scratch RK4 for ODE-02's linear-system and ODE-03's integrator comparisons — real pseudocode for the Euler/RK4 algorithms). Unlike every other block, **this one has no Toussaint-authored source at all**: confirmed in the Phase 1 audit as the one genuine content gap (the user's stated weakest background area, but not covered by any available lecture PDF), so all three modules are tagged `newly-authored`/`external-adapted` throughout and cite standard, universal ODE-textbook treatment (Boyce & DiPrima; Strogatz) generically rather than specific page numbers, since no specific text was actually extracted from. Hours grew modestly from the original 3+3+2=8 estimate to 4+4+3.5=11.5 to match the depth standard set by the other blocks.

---

## Block KIN — Rigid-Body Rotations & Kinematics

Rationale: **revision-paced** per your Edinburgh Advanced Robotics teaching background — kept compact, exercises retained for genuine revision value — **except quaternions**, which stay full-length since you flagged that as your actual weak spot.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| KIN-01 | 3D Rotations & Angular Velocity (revision) | `lecture-robotics` p14–16; `quaternions` [html] Appendix (Rodrigues'/angular-velocity) | Refresh rotation matrices, SO(3), Euler angles/gimbal lock, Rodrigues' formula, $\dot R=\text{skew}(w)R$ — compact, exercise-forward | MATH-03 | 2 | 1 |
| KIN-02 | Quaternions: Exponential/Log Maps, Interpolation & Jacobians (full) | `quaternions` [html], full | Derive the quaternion exponential/log map, SLERP interpolation, and the angular Jacobian w.r.t. quaternion params | KIN-01 | 3 | 1 |
| KIN-03 | Forward/Inverse Kinematics & Jacobians (revision) | `robotkin` [html]; `lecture-robotics` p18–26 | Refresh forward-kinematics chaining, geometric Jacobian construction, and IK as constrained optimization (ties to MATH-02B/OPT-03) — compact | KIN-01, OPT-03 | 3 | 1 |

**Milestone**: folded into the DYN block's milestone (KIN is short and revision-paced by design).

**Production note (Phase 4, batch 5, 2026-08-12)**: KIN-01 and KIN-03 built at the "revision-paced, compact" depth the block was originally scoped for (per the user's own Phase 0 choice, given their Advanced Robotics teaching background), not the exhaustive from-scratch treatment applied to MATH/OPT/PROB. Real content from `lecture-robotics.pdf` (pp. 14-26) and the `quaternions`/`robotkin` HTML notes, two real figures embedded (gimbal-lock illustration, the rotational-DOF Jacobian geometry diagram), real pseudocode for the small-step IK algorithm. KIN-03 deliberately cross-references MATH-02B's Gauss-Newton robotics exercise (the IK closed form is the identical formula) and OPT-03's constrained-optimization framing rather than re-deriving them. `robotkin`'s much deeper dynamics content (Newton-Euler, $M(q)\ddot q+F(q,\dot q)=u$, Euler-Lagrange, the full control stack) was deliberately left out of KIN-03 as out of scope for a kinematics-only module; it belongs to Block DYN instead.

---

## Block DYN — Robot Dynamics & Control

Rationale: Tier 1 core — directly matches your report's Chapter 2.2 (Trajectory Optimisation, MPC) almost equation-for-equation. This is where the ODE primer pays off.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| DYN-01 | Deriving the Equations of Motion: Newton-Euler & Euler-Lagrange | `robotkin` [html] (both derivations) | Derive robot equations of motion two ways (Newton-Euler via Jacobians, Euler-Lagrange) and show they agree | KIN-03, ODE-02 | 3.5 | 1 |
| DYN-02 | PID & Point-Mass Control | `lecture-robotics` p37–41 | Derive the PD/PID closed-form solution; connect damping ratio/time-scale to gain choice | ODE-02 | 2 | 1 |
| DYN-03 | Inverse Dynamics & the Standard Control Stack | `lecture-robotics` p45–49; `robotkin` [html] (control stack) | Derive inverse dynamics from the equations of motion; understand the waypoint→reference-motion→controller stack | DYN-01, DYN-02 | 2.5 | 1 |
| DYN-04 | Optimal Control & Trajectory Optimization | `lecture-robotics` p90–91, p127–135 | Formulate and derive discrete-time optimal control (matches your report's Eq. 2.2 directly); connect to Gauss-Newton (OPT-02) | DYN-03, OPT-04 | 3 | 1 |
| DYN-05 | Splines & Motion Primitives | `splines` [html] | Derive the cubic-spline optimal-control solution; understand Hermite vs. B-spline parameterizations | DYN-04 | 2.5 | 1 |
| DYN-06 | Controllability & Stability | `lecture-robotics` p135–142 | Derive controllability rank conditions; connect Lyapunov-style stability to ODE-02's eigenvalue analysis | DYN-02, DYN-04, ODE-02 | 3.5 | 1 |
| DYN-07 | Model Predictive Control (MPC) | Synthesis module: `lecture-robotics` (finite-horizon optimal control, already covered in DYN-04) + standard receding-horizon control theory (external) | Derive the receding-horizon MPC formulation from DYN-04 and connect explicitly to DYN-06's closed-loop stability framing | DYN-04, DYN-06 | 2.5 | 1 |

**Milestone**: Dynamics & Control Exam (also covers ODE + KIN as prerequisite material). **Obsidian**: `amos_Differentiable_2019` (differentiable MPC) is a strong match for DYN-07.

**Production note (Phase 4, batch 6, DYN block, 2026-08-13)**: Built at the same rigor as the comprehensively-revised MATH/OPT/PROB blocks: formal derivations throughout, independently-verified worked examples (SymPy/NumPy for the pendulum Euler-Lagrange equation, PD/LQR characteristic equations and Riccati solves, cubic-spline coefficients, the operational-space-control second-derivative term, and the LQR-Lyapunov derivation), real pseudocode for RK4-adjacent algorithms and the MPC loop, real figures embedded where they clarify a genuinely new geometric idea (gimbal lock, rotational-DOF Jacobian, damping regimes, joint-vs-operational space). **Four source-formula discrepancies were found via independent verification and corrected, each disclosed in the affected module's Source note**: DYN-03's operational-space-control second-derivative term (source showed a stray factor of 2, corrected via finite-difference check on a 2-link arm), DYN-04's 1D point-mass ARE closed form (a coefficient discrepancy, corrected via direct symbolic ARE solve, verified to machine precision), DYN-05's cubic-spline coefficient `a` (an apparent sign flip, corrected via `sympy.solve` on the exact boundary-value system), and DYN-05's timing-optimal-control closed form for $\tau^*$ (did not reproduce under direct numerical verification in either the general or $v_0=0$ case; rather than assert an unverified formula, the module states the optimization problem precisely and omits the specific closed form). **DYN-06** covers controllability and all three stability definitions plus both worked Lyapunov examples (energy, LQR value function) from `lecture-robotics` pp. 135-142 in full; a fifth discrepancy was found and handled the same way (the source's stated closed-form decay envelope for $\dot V(x)$ in the energy-Lyapunov example did not reproduce cleanly, so only the verified sign result $\dot V=-K_d\dot q^2$ is asserted, using the standard total-mechanical-energy Lyapunov function rather than the source's kinetic-energy-only shorthand). **DYN-07** is a **newly-authored synthesis module**: `lecture-robotics.pdf` contains no section literally labeled MPC or "receding horizon" (confirmed via full-text search), so the module builds the receding-horizon principle directly from DYN-04's already-covered finite-horizon trajectory optimization plus standard control-theory MPC concepts (cited externally, Rawlings/Mayne/Diehl). The original plan called for a direct cross-check against Definition 3 of the user's own PhD Year-1 report; that report was not accessible as a file within this project's approved source directories this session, so the cross-check was **omitted rather than fabricated** — flagged explicitly in DYN-07's Source note and to the user, revisable if the report is made available. Total DYN block time: ~27 hrs (up from the original ~19.5 hr estimate, matching the same depth-driven growth seen in every other comprehensively-built block).

---

## Block PLAN — Path & Motion Planning

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| PLAN-01 | Configuration Space & Geometric Feasibility | `robotkin` [html] (Fundamental Kinematics Concepts); `lecture-robotics` p52–68 | Formalize $\mathcal{X}$/$\mathcal{X}_\text{fea}$ ($C_\text{space}$/$C_\text{free}$/$C_\text{obs}$) — matches your report's Definition 1 exactly | KIN-03 | 3 | 1 |
| PLAN-02 | Sample-Based Path Finding (RRT/PRM family) | `lecture-robotics` p68–81 | Derive and compare sampling-based planners; understand completeness/optimality trade-offs | PLAN-01 | 3.5 | 1 |
| PLAN-03 | Non-Holonomic Systems | `lecture-robotics` p81–89; `robotkin` [html] (holonomic/non-holonomic constraints) | Distinguish holonomic/non-holonomic/integrable constraints; derive a simple non-holonomic example (car model) | PLAN-01, DYN-01 | 3.5 | 1 |
| PLAN-04 | Path Optimization | `lecture-robotics` p90–91 (synthesis with DYN-04) | Connect sampling-based paths to trajectory optimization (DYN-04) as a refinement step | PLAN-02, DYN-04 | 2 | 1 |

**Optional/reference branch (Tier 4, off this block)**: SLAM & State Estimation — `lecture-robotics` p93–125. Confirmed not needed for your work; kept as a single compact "know what it is" reference module, not a full sub-course. **Obsidian**: `ortiz-haro_iDbA_2025` (kinodynamic motion planning) is a strong match for PLAN-02/PLAN-04.

**Production note (Phase 4, batch 7, PLAN block, 2026-08-13)**: Built at the same rigor as MATH/OPT/PROB/DYN: formal definitions throughout, real pseudocode (PRM generation, both RRT variants, control-based sampling, RRT with differential constraints — all reproduced faithfully from the source with citations), four real figures embedded (PRM local-planner resolution, an RRT growth tree, and a bad-vs-good car-path comparison pair), all numeric/symbolic claims independently verified (the car's non-holonomic constraint via SymPy, the PRM probabilistic-completeness bound via direct computation showing it goes vacuous for a narrow-passage clearance). PLAN-01's formal $\mathcal{X}$/$\mathcal{X}_\text{fea}$ treatment (the curriculum's originally-flagged "matches your report's Definition 1" objective) was sourced directly from Marc Toussaint's own `robotkin` HTML note ("Fundamental Kinematics Concepts" section), previously unused by KIN-01/KIN-03 — no external (Lynch & Park) sourcing was actually needed, since `lecture-robotics.pdf` itself has no literal "configuration space" terminology but `robotkin` does define the equivalent $\mathcal{X}/\mathcal{X}_\text{fea}$ concept formally. PLAN-04 was deliberately kept short and purely connective, per its own curriculum-scoped role: it does not re-derive DYN-04's trajectory-optimization cost function, only explains why sample-based path finding (PLAN-02/03) and trajectory optimization (DYN-04) are needed together (global-topology vs. local-refinement), with a capstone-style synthesis exercise tracing the full PLAN/DYN pipeline. Total PLAN block time grew from the original ~8 hr estimate to ~12 hrs, matching the depth-driven growth pattern of every other comprehensively-built block.

---

## Block MANIP — Manipulation & Legged Locomotion Foundations

Rationale: elevated per your correction — relevant despite the source's own "SKIPPED THIS TERM" label. Naturally light given how brief the source treatment is.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| MANIP-01 | Grasping Fundamentals: Contacts & Force Closure | `lecture-robotlearning` p81–83 (Grasping Background/Contacts & Force Closure); `lecture-robotics` p157–162 (brief intro) | Understand contact modeling and the force-closure condition for stable grasps | DYN-01 | 2.5 | 2 |
| MANIP-02 | Legged Locomotion Basics | `lecture-robotics` p163–177 (brief intro) | Understand the basic gait/contact-sequencing view of legged locomotion | DYN-01, PLAN-03 | 3 | 2 |

**Obsidian**: `corberes_Complete_2025` (quadruped locomotion) is a strong match for MANIP-02; `zhu_Should_2025` (contact-rich manipulation) for MANIP-01.

**Production note (Phase 4, batch 8, MANIP block, 2026-08-13)**: Built at the same rigor as the rest of Phase 4, appropriately scoped to this block's own "naturally light" framing (the source material itself is a brief, explicitly-skipped-that-term intro, not a full sub-course). MANIP-01's formal contact/friction-cone/force-closure definitions were sourced primarily from `lecture-robotlearning`'s "Grasping Background" section (a more complete formal treatment than `lecture-robotics`'s own brief version of the same material), with the Mishra-Schwartz-Sharir existence result, the wrench-polytope quality metric, form closure/caging/equilibrium, and the mitten thought experiment from `lecture-robotics`. MANIP-02 covers statically-stable walking, the Zero Moment Point (explicitly connected to DYN-03's fully-actuated operational-space control via the "glued foot" argument), and passive dynamic walking (the Compass Gait's swing-phase dynamics shown to be exactly DYN-01's general form with $u=0$, plus the standard impact-model assumptions and a brief forward-pointer to policy-gradient-based walking). Three real figures embedded (friction cones, the mitten thought experiment, the Compass Gait mechanical model). All numeric claims independently verified (a 2D wrench computation via NumPy, a barycentric-coordinate support-polygon stability check via NumPy). Total MANIP block time grew modestly from the original ~4.5 hr estimate to ~5.5 hrs.

---

## Block ML — ML & Autodiff Bridge

Rationale: this is the block that directly answers your Phase 0 ask (manual MLP-by-hand ↔ PyTorch). `lecture-ai` §1.1/§1.3 and `lecture-machinelearning` are now primary sources (audit finding — this used to be a gap requiring new authoring).

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| ML-01 | Computation Graphs & Automatic Differentiation | `lecture-ai` p5–9 (§1.1) | Derive forward/backward chain rule via computation graphs; connect to autograd | MATH-02 | 3 | 1 |
| ML-02 | Regression & Classical Supervised ML | `lecture-machinelearning` p12–31 | Derive least-squares regression and logistic regression from first principles | OPT-01, PROB-01 | 3.5 | 1 |
| ML-03 | Neural Networks: Manual Forward/Backward Pass ↔ PyTorch | `lecture-maths` p19–21 (Ex.4/5 Backprop); `lecture-ai` p28–36, p206–208 (Ex.1–2) | **Compute a small MLP's forward+backward pass entirely by hand, then reproduce it exactly in PyTorch line-by-line** | ML-01, ML-02 | 3.5 | 1 |
| ML-04 | Neural Network Architecture Essentials | `lecture-ai` p36–45 (CNN, LSTM, init, regularization, dropout, SGD) | Understand architectural building blocks and why each regularization trick exists | ML-03 | 3 | 1 |
| ML-05 | Kernelization & Bayesian Learning | `lecture-machinelearning` p50–53, p85–92; cross-ref `OPT-06`'s GP formulas | Derive the kernel trick and ridge regression as Bayesian inference; show GPs = Bayesian kernel ridge regression | ML-02, PROB-05 | 4 | 1 |
| ML-06 | Unsupervised Learning: PCA, Embeddings & Clustering | `lecture-machinelearning` p54–71 | Derive PCA via projection-error minimization (connects to MATH-04's eigendecomposition); derive k-means/GMM-EM | MATH-04, ML-02 | 3.5 | 1 |
| ML-07 | Advanced Topics: Ensembles, Bayesian NNs & No Free Lunch | `lecture-machinelearning` p72–84, p92–94 (Tier 3, optional) | Survey ensemble/local learning and Bayesian neural nets at a conceptual level | ML-04, ML-05 | 2.5 | 3 |

**Milestone**: ML Foundations Exam.

**Production note (Phase 4, batch 9, ML block, 2026-08-13)**: Built the remaining 6 modules (ML-01, ML-02, ML-04 through ML-07; ML-03 already existed as the Phase-3 pilot) at the same rigor as the rest of Phase 4. Deliberate care was taken to avoid duplicating content already derived elsewhere in the workbook, since ML naturally overlaps heavily with MATH/OPT/PROB: ML-01 covers computation graphs/forward-vs-backward-mode chain rule (genuinely new, not previously formalized despite ML-03 assuming it); ML-02 cross-references MATH-02B's already-derived ridge-regression closed form rather than re-deriving it, adding non-linear features/estimator-variance/lasso/cross-validation and deriving binary logistic regression's model and gradient from scratch (**this also fixed a real, previously-undetected dangling cross-reference**: MATH-02B said "recall the logistic regression gradient from ML-03," but ML-03 never actually covers logistic regression, only MLP forward/backward passes with square loss; the link now correctly points to ML-02, which supplies that gradient); ML-04 cross-references OPT-05 for SGD/Adam rather than re-deriving, covering NN-specific architecture content instead (linear/nonlinear operators, CNN/LSTM/Transformer survey, initialization, dropout); ML-05 cross-references OPT-06's already-stated GP posterior formulas, supplying the derivation OPT-06 assumed (the kernel trick, RKHS/representer theorem, and — the module's central insight — deriving ridge regression as Bayesian inference to show precisely why "Gaussian Process" and "Bayesian kernel ridge regression" name the same object); ML-06 cross-references MATH-04's eigendecomposition-PCA connection, adding the projection-error-minimization derivation that explains *why* eigendecomposition solves PCA, plus k-means and GMM/EM; ML-07 cross-references OPT-05's No-Free-Lunch theorem for the general case, covering local/ensemble learning and dropout's approximate-Bayesian justification (connecting ML-04 and ML-05) at an appropriately lighter, Tier-3 survey depth matching the source's own "optional/advanced" framing for much of this content (AdaBoost, decision trees, random forests, ICA, kernel PCA's full derivation, spectral clustering, MDS are all surveyed by name/idea rather than fully derived). All numeric/symbolic claims independently verified (SymPy for the polynomial-kernel feature-map identity, the logistic-regression gradient, and the partial/total-derivative example; NumPy for the Bayesian-posterior-equals-ridge-estimate claim, the k-means iteration, the RBF feature value, and the AdaBoost weight). Total ML block time grew from ~19 hrs (original estimate across all 7 modules) to ~23 hrs.

---

## Block RL — Reinforcement Learning Foundations

Rationale: Tier 1, `lecture-ai` Part 2 is now the primary source (rigorous, with convergence proofs — reduces reliance on the external Sutton & Barto supplement to optional depth, though it's still a great cross-check since your own report cites it).

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| RL-01 | MDPs, Value Functions & Bellman Equations | `lecture-ai` p52–55, p67–71 | Formalize MDPs and derive the Bellman optimality equation, generalizing DYN-04's deterministic Bellman equation to stochastic transitions/rewards | PROB-01, DYN-04 | 3 | 1 |
| RL-02 | Value Iteration, Q-Iteration & Convergence | `lecture-ai` p71–72 | Derive value/Q-iteration and prove convergence (contraction-mapping argument) | RL-01 | 3 | 1 |
| RL-03 | Model-Free RL: TD Learning & Q-Learning | `lecture-ai` p71–80 | Derive TD-learning and Q-learning updates, with convergence proof | RL-02 | 3 | 1 |
| RL-04 | Policy Gradient & Deep RL | `lecture-ai` p76–81; `lecture-robotlearning` p60–65 (robotics-flavored bridge) | Derive the Bellman residual loss and the policy gradient theorem (log-derivative trick); survey deep-RL tricks | RL-03, ML-04 | 3.5 | 1 |
| RL-05 | Exploration Strategies | `lecture-ai` p82–89 | Understand the model-based/model-free/policy-search taxonomy, UCB-style exploration (cross-ref OPT-06), R-max, and Bayesian RL | RL-03 | 2.5 | 1 |
| RL-06 | Monte Carlo Tree Search & UCT | `lecture-ai` p52–66 (incl. compact tree-search prereq p112–121) | Derive UCT from basic tree search + UCB1 (OPT-06) — directly relevant to your MCTS-based literature | RL-02, OPT-06 | 3.5 | 2 |
| RL-07 | Decision Theory & Value Alignment (light touch) | `lecture-ai` p90–98 | Understand decision networks (unifying MDPs and ML's empirical risk minimization) and the value-alignment framing at a conceptual level | RL-04 | 2 | 1 |

**Milestone**: RL Foundations Exam. **Optional/reference branch (Tier 3)**: LM Reasoning/RLHF & Explainable AI — `lecture-ai` p99–111.

**Production note (Phase 4, batch 10, RL block, 2026-08-13)**: Built all 7 modules at the same rigor as the rest of Phase 4. RL-01's formal MDP definition is explicitly derived as DYN-04's deterministic Bellman equation generalized (stochastic transitions replacing deterministic dynamics, reward-maximization replacing cost-minimization); RL-02's Q-iteration convergence proof is a full contraction-mapping argument, presented in complete detail since it's the template RL-03's Q-learning convergence explicitly rides on (as a stochastic approximation). RL-04's policy gradient theorem is derived via the log-derivative trick through to the trajectory-probability factorization (the causality argument dropping cross-terms is cited, not fully re-derived, matching the source's own level of detail); its Bellman residual loss deliberately connects to ML-02/ML-04/OPT-05's already-covered regression/SGD machinery rather than treating neural Q-function training as new content. RL-05 and RL-06 both deliberately cross-reference OPT-06's UCB1 formula rather than re-deriving it: RL-05 for Q-value exploration bonuses, RL-06 for UCT's tree policy (shown explicitly to be UCB1 run independently at every tree node, the module's central result); RL-06 also cross-references PROB-02's Monte Carlo estimator. RL-07 is deliberately kept at a lighter "light touch" depth per its own curriculum scope, closing the block by showing MDPs (RL-01) and supervised ML's empirical risk minimization (ML-02) are both instances of the same decision-network formalism. All numeric/symbolic claims independently verified (Python for the value-iteration/Q-iteration hand examples, the UCT node-selection comparison, the tree-search node counts, the risk-aversion example; SymPy for the log-derivative trick and trajectory-factorization identities). Total RL block time grew from ~16.5 hrs (original estimate) to ~20.5 hrs.

---

## Block SYM — Symbolic Foundations for TAMP

Rationale: elevated per your correction — this directly feeds TAMP/Logic-Geometric Programs, matching your report's task-level/motion-level split (§2.1.3) and Toussaint's own long-horizon planning approach. Scoped narrowly: enough logic to understand FOL/PDDL, not general classical-AI breadth.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| SYM-01 | Propositional Logic Essentials | `lecture-ai` p163–181 (scoped: syntax/semantics/inference basics only) | Understand propositional syntax/semantics/inference as the minimum needed for first-order logic | — | 1.5 | 2 |
| SYM-02 | First-Order Logic & Inference | `lecture-ai` p182–194 | Derive unification and forward/backward chaining for FOL | SYM-01 | 3 | 1 |
| SYM-03 | STRIPS, PDDL & Relational MDPs | `lecture-ai` p195–199 | Formalize STRIPS-style operators and PDDL, connecting symbolic actions to MDP transitions | SYM-02, RL-01 | 2.5 | 1 |
| SYM-04 | Probabilistic Relational Models & Markov Logic Networks | `lecture-ai` p199–205 | Understand PRMs/MLNs as a bridge between logic and probability | SYM-03, PROB-01 | 1.5 | 2 |

**Optional/reference branches (Tier 4, off this block)**: general CSP-solving techniques (`lecture-ai` p128–137) and graphical models beyond what SYM-04 needs (`lecture-ai` p138–162) — compact reference links, not full modules. A minimal tree-search treatment is instead placed in RL-06 where it's actually used (MCTS).

**Production note (Phase 4, batch 11, SYM block, 2026-08-13)**: Built all 4 modules at the same rigor as the rest of Phase 4, closing the last block feeding directly into TAMP. SYM-01 kept deliberately narrow (its own curriculum-scoped "basics only" role) — full propositional syntax/semantics/entailment/soundness/completeness/Horn-clause Modus Ponens, with the full resolution algorithm and CNF-conversion procedure deliberately deferred to SYM-02's richer first-order treatment rather than duplicated. SYM-02 covers FOL syntax/semantics, quantifier properties, universal/existential instantiation, unification (with newly-authored worked examples structurally analogous to but distinct from the source's own King/Greedy/Evil and "West is a criminal" running examples, so a fully-solved version of the source's own example isn't presented before the reader attempts it), Generalized Modus Ponens, and FOL resolution. SYM-03 represents MDP states as grounded FOL literals (cross-referencing RL-01), covers STRIPS/PDDL and the frame assumption, and makes the case (with a verified state-space-size calculation, $2^{400}\approx10^{120}$ for just 20 objects) for why deterministic rules are unlearnable, motivating Pasula/Zettlemoyer/Kaelbling's probabilistic relational rules and the "uncertainty enables learning" insight (explicitly connected to ML-02's regularization). SYM-04 covers PRMs and Markov Logic Networks (including the Friends & Smokers worked example and both of MLNs' limiting cases: zero-arity predicates recovering standard graphical models, infinite weights recovering classical hard FOL), closing with an explicit statement of the whole block's throughline (structure enables generalization; determinism is unlearnable; calibrated uncertainty is what makes learning tractable). All numeric/symbolic claims independently verified (Python for the state-space-size and MLN-grounding/factor-value computations).

---

## Block RLEARN — Robot Learning (capstone-adjacent)

Rationale: the payoff block — `lecture-robotlearning`'s advanced/applied content has no equivalent elsewhere in the corpus and is the section most directly aligned with your own research and your advisor's work.

| ID | Title | Source & pages | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| RLEARN-00 | Robot Learning: Taxonomy & Landscape | `lecture-robotlearning` p13–17 (§1.2, "Mini Taxonomy") | Orient across the whole block with Toussaint's own "what is learned × how is data generated" taxonomy before diving into individual techniques | DYN-04, ML-04 | 2 | 1 |
| RLEARN-01 | Dynamics Learning | `lecture-robotlearning` p23–34 | Understand parameter estimation, dynamics regression, and residual dynamics learning | DYN-01, ML-04 | 3 | 1 |
| RLEARN-02 | Imitation Learning | `lecture-robotlearning` p35–56 | Derive behavior cloning; understand DAgger, distributional shift, and generative approaches (GAN/VAE/diffusion policies) | ML-04, RL-04 | 4 | 1 |
| RLEARN-03 | RL in Robotics: Reward Engineering & Deep RL Tricks | `lecture-robotlearning` p57–65 | Apply RL-04's foundations to robotics-specific reward design and data collection challenges | RL-04 | 2 | 1 |
| RLEARN-04 | Offline RL & Sim2Real | `lecture-robotlearning` p66–72 | Understand offline RL regularization and domain randomization for sim-to-real transfer | RLEARN-03 | 3 | 1 |
| RLEARN-05 | Inverse RL | `lecture-robotlearning` p73–80 | Derive max-margin and max-entropy IRL formulations | RL-04 | 3 | 1 |
| RLEARN-06 | Manipulation & Grasp Learning | `lecture-robotlearning` p81–87 | Connect MANIP-01's force-closure foundations to learned grasp-quality prediction | MANIP-01, RLEARN-01 | 2.5 | 1 |
| RLEARN-07 | Task and Motion Planning & Logic-Geometric Programs | `lecture-robotlearning` p88–99 | Synthesize SYM's symbolic tools with PLAN's motion planning into a full TAMP formulation — **the module most directly aligned with your research** | SYM-03, PLAN-02, OPT-04 | 4 | 1 |
| RLEARN-08 | Multi-Robot Learning (light touch) | `lecture-robotlearning` p100–114 (Tier 3, optional) | Survey Deep Sets/GNN/MARL approaches at a conceptual level | RLEARN-04 | 2 | 3 |

**Milestone**: Robot Learning Exam — the last milestone before the capstone. **Obsidian**: strong matches throughout — `peng_ASE_2022` (skill embeddings, RLEARN-02), `zhu_Should_2025` (RLEARN-06), `toussaint_NLP_2024` (RLEARN-07 — your advisor's own sampling-based TAMP paper), `dambrosio_ACHIEVING_2025` (RLEARN-03).

**Production note (Phase 4, batch 12, RLEARN block, 2026-08-13)**: Built all 9 modules (8 originally planned + RLEARN-00, inserted following the MATH-00 precedent). **RLEARN-00 insertion**: in response to the user's own "starting outline" suggestion (types of RL, robot learning taxonomy), full-text search of `lecture-robotlearning` located Toussaint's own "Mini Taxonomy" (§1.2, p13–17), which directly answers that need with real source content rather than invented framing: two independent axes (what is learned: dynamics/policy/reward/plan/value/state/constraints; how is data generated: demonstration/online/simulation/optimal/safe), a table mapping every taxonomy cell to its covering module, and the "robot learning as shortcuts to the model-based control stack" framing, explicitly scoping out the source's own redundant "Robotics Essentials"/"ML Essentials" primers (fully covered already by DYN/KIN and ML). Heavy cross-referencing discipline throughout, given RLEARN sits on top of nearly the whole curriculum: RLEARN-01 cross-refs ML-02 (estimator variance) and PROB-06 (state-space ELBO); RLEARN-02's distributional-shift/compound-error theorem (Ross, Gordon & Bagnell 2011) is stated and explained rather than re-derived, matching the source's own level of detail; RLEARN-03 is deliberately short, explicitly deferring MDP/Bellman/policy-gradient recap to Block RL; RLEARN-05's Adversarial IRL discriminator decomposition is explicitly connected (a newly-authored, not-in-source synthesis) to RLEARN-03's reward-shaping formula via RL-04's Bellman equation, showing $f_{\theta,\phi}\to A(s,a)$; RLEARN-06 and RLEARN-07's Deep Visual Constraints are both explicit instances of RLEARN-02's privileged-teacher pattern; RLEARN-07's Deep Visual Reasoning policy is connected term-by-term to a language model's next-token prediction. All numeric exercises independently verified (Deep Sets aggregation, reward-shaping fixed point via random-MDP value iteration, Bradley-Terry preference probability, ACRONYM grasp counts). Block hours grew from the original 20–24 estimate to ~25.5 across 9 modules. Full validation (JSON id/hint-count matching, site-wide `<ExerciseCard>` cross-reference — 289/289 exercises matched two-way — and `$`-fence scan) passed clean; one schema bug (a stray empty `notation: [{}]` placeholder in RLEARN-00 and RLEARN-06's frontmatter, left over from drafting) was caught by `astro check` and fixed. `npm run build` (68 pages) and a site-wide `katex-error` dist scan both passed clean; live-browser-verified RLEARN-00, RLEARN-07, and RLEARN-08, including one numeric exercise (Deep Sets aggregation) passed through the real client-side grading pipeline end-to-end.

---

## Block CAP — Capstone: Research Bridge

Rationale: rather than an invented capstone project disconnected from your actual work, this connects the whole curriculum to your own CDT-D2AIR project (manipulation planning over convex decompositions of $C_\text{free}$, per your Year-1 report Chapter 5). It's framed as a synthesis/application exercise, not new taught content.

| ID | Title | Source | Objective | Prereqs | Hrs | Tier |
|---|---|---|---|---|---|---|
| CAP-01 | Research Bridge: Convex-Decomposition Manipulation Planning | Newly authored, scaffolded against your own report §5.1 (with your permission to reference it) | Walk through your own planner's pipeline (convex decomposition of $C_\text{free}$ → optimization-based planning) using the curriculum's own notation, as a self-check that the taught material actually transfers to your research | RLEARN-07, DYN-04, PLAN-01 | 3 | 1 |

Followed by the **Cumulative Final Assessment** (see below) — not a new module, the closing milestone of the whole route.

**Production note (Phase 4, batch 13, CAP block + site wiring, 2026-08-13)**: Built CAP-01, the curriculum's last content module. The user's PhD Year-1 report (`Final_Scientific_Report_Julia Lopez Gomez.pdf`) had been inaccessible in every prior batch that referenced it (DYN-07's originally-planned cross-check was omitted for this reason); the user added it to `original notes/` mid-session and confirmed via a plain-text file path. Read its Table of Contents plus §5.1 (pp.29–35, "Optimisation-Based Manipulation Planner in Convex Decompositions of $C_\text{free}$") in full. CAP-01 walks through that section's actual pipeline (composite configuration space $CS=C_\text{rob}\times C_\text{obj}$, transit/transfer paths, grasp/placement spaces $CG$/$CP$, the three-stage convex-decomposition/MIQP/GCS pipeline including the big-M region-membership constraint, and the 3-DOF/4-DOF bottle-cap experimental results table) rather than inventing a generic capstone exercise. Explicitly cross-references PLAN-01 (the report's $CS$/$CS_\text{free}$ notation is shown to be identical to PLAN-01's $\mathcal{X}$/$\mathcal{X}_\text{fea}$, just specialized to a composite system), DYN-04 (the MIQP's quadratic path-length objective), and RLEARN-07 (a newly-authored structural-parallel table mapping the MIQP's discrete/continuous split onto RLEARN-07's TAMP skeleton/$\bar\phi$/$\hat\phi$ formulation, explicitly noting where the analogy holds and where it breaks down, since the report's own MIQP searches a larger, more geometrically explicit discrete space than a hand-defined symbolic skeleton). Cited via `externalSources` (not `sources`, which is reserved for the Toussaint lecture corpus) since this is the user's own document, not part of the manifest. All 5 exercises newly authored; numeric ones (the big-M minimal-value derivation, a pipeline-time sum from the results table) independently verified by direct computation. Full validation (JSON id/hint-count matching, site-wide `<ExerciseCard>` cross-reference — 294/294 exercises matched two-way across all 67 modules — `$`-fence scan) passed clean; `astro check` and `npm run build` (73 pages) both clean; site-wide `katex-error` scan 0 matches; live-browser-verified including one numeric exercise through the real grading pipeline.

**Same batch — site navigation/discovery layer wired up**: per the user's request to finish the site now that content is essentially complete, built the pieces `data/curriculum/ARCHITECTURE.md` specified but deferred until "enough real content exists": a `/curriculum` roadmap page (all modules grouped by block, generated at build time from the content collection so it can't drift, with tier badges, hours, prerequisite links, block-jump navigation, and a client-side filter box, no new dependency, matching the architecture doc's own "simple built index" fallback over a heavier Pagefind-style search); `/concepts`, `/notation`, and `/sources` index pages (aggregated from every module's `concepts[]`/`notation[]`/`sources[]`/`externalSources[]` fields); prev/next navigation on every module page (linear route order = block order then module ID, wired through `[block]/[id].astro` into `ModuleLayout`); and a real fix for a previously-dead link (the breadcrumb's block link 404'd, since no `/course/{block}` route ever existed — repointed to `/curriculum#{block}`). Also fixed a real display bug caught during this work: `ModuleLayout`'s footer unconditionally rendered an empty "Sources cited" section for any module using only `externalSources` (i.e. just-built CAP-01), and never rendered `externalSources` at all; added a conditional `externalSources` section so the citation actually appears. Homepage rewritten to link to the new pages instead of a flat 66-module list, and its own "not yet wired into navigation" disclaimer removed now that it's wired. **Deliberately not built this batch** (present in `ARCHITECTURE.md` but out of scope for "wiring the curriculum," not requested): Pagefind-style fuzzy search (the filter box covers the practical need without a new dependency), the print/book-export concatenation script, Anki export, and milestone-exam modules — all flagged here as consciously deferred, not silently dropped, should the user want them later. README updated with a new "Editing content" section (fix text, add math with the multi-line-`$`-fence gotcha called out, add a `SourceFigure` image, add a link, add/wire an exercise with the `<ExerciseCard>` gotcha called out, add a whole new module or block, and the exact validation commands to run afterward) so the user can self-serve edits without needing an AI session for every small change.

**Production note (Phase 4, batch 14, milestone-exam + cheat-sheet infrastructure, 2026-08-14)**: User flagged that milestone exams and per-block cheat sheets, both speced in this document and in `ARCHITECTURE.md`, were never actually built (only the Phase-3-pilot's single ML-03 cheat sheet existed; zero exams). Built the infrastructure for both, then the first real instance of each as a proof-of-pattern: a new `milestones` content collection (`src/content.config.ts`, schema `{id, block, title, coversModules, masteryThreshold, remediationMap}`), reusing the existing `questions`/`solutions` collections for exam exercises (moduleId `{BLOCK}-EXAM`) rather than inventing new grading machinery — this means every exam gets deterministic/symbolic/rubric grading, hints, and the Review-with-Claude packet for free, identical to module exercises. Added an optional `retakeVariantOf` field to the `questions` schema. New `MilestoneLayout.astro` and `/exam/[block]` route; `masteryThreshold` is presented explicitly as a self-assessment target, not an enforced gate, since there's no server to compute a real score. **MATH-EXAM built as the reference implementation**: 4 primary exercises (conceptual, derivation, calculation/modeling, transfer) + 4 retake variants, each deliberately cumulative (combining 2+ MATH modules, or extending a taught identity into territory the modules themselves didn't work out) rather than repeating module-level exercises — e.g. the transfer exercise derives weighted-least-squares and general-PSD-regularizer closed forms, checked against MATH-02B's own worked examples first to confirm they weren't duplicates. All numeric/derivation claims independently verified (eigendecomposition/rank-1-approximation-error for two contrasting matrices, Hessian-as-Jacobian-of-gradient via finite differences, vector-valued Taylor via finite differences, both closed-form gradients via direct numerical residual checks). **MATH cheat sheet built as the reference implementation** (`src/content/cheatsheets/math.mdx`, id `math`, one per block per the user's chosen granularity): condensed Definitions/Core equations/Assumptions/Common mistakes/Decision rule sections spanning all 8 MATH modules, linked bidirectionally (all 8 modules' `cheatsheetLinks` updated to point to it). A `/cheatsheets` index page was also built (previously a dead breadcrumb link with no actual index, another pre-existing gap). `/curriculum` now shows a "📝 Milestone exam" / "📄 Cheat sheet" link per block section whenever one exists, generated automatically from the content collections (no per-block hardcoding needed as more are added). All validation passed clean (astro check, build — 76 pages, katex-error scan, 302/302 exercises matched two-way including the new milestone exercises); live-verified the exam's remediation-map links and both its numeric exercises through the real grading pipeline. **Remaining, not yet built**: milestone exams for OPT/PROB/DYN/ML/RL/RLEARN + the cumulative final assessment after CAP (6 more, all confirmed in scope by the user); cheat sheets for the other 12 blocks (confirmed in scope, "one per block" granularity); the two cumulative-review modules before RLEARN/CAP (explicitly not selected by the user this batch); an Anki-export feature (proposed as auto-generated from existing `notation[]`/retrieval-question content rather than hand-authored flashcards or an in-browser SRS game — user hasn't confirmed yet).

**Production note (Phase 4, batch 15, 2026-08-14)**: All 13 per-block cheat sheets are now built (`src/content/cheatsheets/{blockid}.mdx`, lowercase block ID), each cross-checked against that block's actual module notation/equations rather than reconstructed from memory. Milestone exams: MATH, OPT, and PROB are built (3 of 7); DYN, ML, RL, RLEARN + the cumulative final after CAP remain. Found and fixed a real MDX-build-breaking bug (`plan.mdx`'s cheat sheet table): a raw `|` inside a LaTeX absolute-value expression inside a markdown table cell broke table parsing — `astro check` didn't catch it, only `npm run build` did. Fixed with `\vert`; swept the whole site for the same pattern, found no other instances.

**Production note (Phase 4, batch 16, 2026-08-14 — Assessment Schedule complete)**: The remaining 4 block exams (DYN, ML, RL, RLEARN) and the Cumulative Final Assessment (routed at `/exam/CAP`, since it's the closing milestone after CAP rather than a separate per-block exam) are now built, completing the full Assessment Schedule exactly as originally speced below — every block that gets a milestone now has one, every block that doesn't (ODE/KIN/PLAN/MANIP/SYM, folded into DYN/RLEARN) correctly has none. Every exam stayed genuinely cumulative: DYN-EXAM connects LQR to Lyapunov stability and derives feedback-linearization's error dynamics as a new instance of DYN-02's PD form; ML-EXAM derives and verifies the push-through identity connecting primal/dual ridge regression; RL-EXAM derives a policy gradient from scratch and shows why nonlinear utility breaks the Bellman recursion's linearity; RLEARN-EXAM numerically verifies RLEARN-05's own AIRL-equals-advantage-function claim on concrete MDPs (not just re-asserting it). The Cumulative Final's 8 questions deliberately span the *whole* curriculum rather than any single block, including a self-check reflection question asking the student to name 4+ blocks CAP-01 actually depends on. The Anki-export feature (`/anki`, `/anki-export.tsv`) was also built this batch: 208 notation cards (universal, all 67 modules) plus 39 curated exercise-recall cards, deliberately not attempting to auto-generate answers for Retrieval Questions (no canonical answer exists in the source for those). All numeric/derivation claims across all 5 new exams independently verified (NumPy for DYN's eigenvalues/controllability, ML's push-through identity, RLEARN's value-iteration-based AIRL check; finite differences for RL's policy gradient).

---

## Assessment Schedule

- **Entry diagnostic**: short, taken before MATH-01, to calibrate which Foundation-tier exercises can be skipped (e.g. if you're already comfortable with a specific derivative rule). Not a gate — informational only.
- **Lesson checks**: 1–2 quick retrieval questions inline after each module's core exposition (per the module-format spec) — not separately scheduled, built into every module.
- **Module mastery checks**: end of every module (per the mastery-criteria policy above).
- **Milestone examinations**: after each block, per the block tables above (MATH, OPT, PROB, DYN [covers ODE+KIN], ML, RL, RLEARN). PLAN/MANIP/SYM don't get standalone exams — their material is folded into the DYN and RLEARN milestones respectively, since they're shorter blocks that feed directly into those. Each milestone: conceptual explanation + derivation + calculation/modeling + one transfer-to-unfamiliar-example problem + a retake variant, per spec.
- **Cumulative reviews**: a short cumulative-review module inserted before RLEARN (reviewing MATH/OPT/PROB/DYN/KIN/ML/RL/SYM together, since RLEARN draws on all of them) and again before CAP.
- **Capstone**: CAP-01 (Research Bridge) followed by the **Cumulative Final Assessment** — mixed conceptual/derivation/modeling problems spanning the full route, with a remediation map linking any missed question back to its originating module.

**Production note (Phase 4, batch 17, 2026-08-14 — original design fully complete)**: The two cumulative-review modules above are now built as `REV1-01` (block `REV1`, inserted between SYM and RLEARN) and `REV2-01` (block `REV2`, inserted between RLEARN and CAP) — see `src/lib/curriculum/blocks.ts`. Both are pure synthesis of already-built, already-verified content (no new source material); REV2-01 is deliberately shorter since RLEARN-00 already provides orientation at that block's own start. This same batch also built the print/book export (`/print`, a single Astro page rendering every module in curriculum order rather than a separate concatenation script) and Pagefind full-text search (`/search`, indexing the production build; needs `npm run build && npm run preview`, not `npm run dev` — documented in `README.md`). With this batch, every item in this document and in `ARCHITECTURE.md`'s original design is built and validated; nothing from the original curriculum/architecture scope remains outstanding.

---

## Open Decisions Before Phase 3 (Pilot)

1. ~~**ODE external source**~~ — **Resolved (Phase 4, batch 4)**: no Lynch & Park PDF (or any external source PDF) is actually available locally in this project; Block ODE was authored as fully newly-authored content following standard, universal ODE-textbook treatment (cited generically as Boyce & DiPrima / Strogatz, not page-specific, since nothing was literally extracted from either), consistent with this block's `newly-authored` classification.
2. **Pilot module choice**: I'd propose **ML-03** (Neural Networks: Manual Forward/Backward Pass ↔ PyTorch) as the pilot — it's explicitly what you asked for in Phase 0, and structurally hits every pilot requirement (derivation, worked example, numeric + symbolic exercise, tiered hints, PyTorch cross-check, cheat-sheet link, mastery check). Alternative: **KIN-02** (Quaternions), your explicitly flagged weak spot, also derivation-rich. Let me know which you'd rather see first, or if you'd prefer a different module entirely.
3. Everything else in this document is presented for your approval — happy to adjust block boundaries, merge/split modules, or reorder before I lock this in and move to the technical architecture + pilot.
